# Wizard Vision Brain Configuration Field Guide

## Purpose and scope

This is the living, evidence-based guide for configuring Wizard Vision brains. It records choices made through brain identities, curricula, benchmarks, and genetic searches that improved learning, retention, integration, inference, or efficiency.

This document deliberately excludes core neurofabric implementation details. A core repair belongs here only when it changes a practical configuration limit or invalidates earlier configuration advice.

Future work must revise or remove guidance when stronger evidence contradicts it. Do not preserve a recommendation merely because it was once useful.

## Evidence labels

- **Proven live**: reproduced on the persisted programming brain and protected by a read-only benchmark.
- **Experiment-supported**: measured in a controlled comparison or genetic search, but not yet broadly replicated.
- **Provisional**: a useful working hypothesis that still needs an ablation or broader search.
- **Rejected**: tested and found harmful, misleading, or unnecessarily expensive.

## Current known-good principles

### Preserve atoms and add derived views in parallel

**Proven live.** Raw character/byte sensory pools remain the grounding substrate. Feature extraction must add parallel pools; it must not replace raw input with conventional tokens. Derived pools may represent source structure, instruction intent, execution outcome, repair delta, environment, or other stable features. Their co-firing bindings retain a path back to the atom-level episode.

This permits hierarchical compression into motifs and concepts while preserving sensitivity to deviations such as typos, malformed syntax, and small contextual changes.

### Use several feature-specific pools and bind their simultaneous evidence

**Proven live.** A coding episode is more useful when its distinct signals enter separate pools and co-fire in the same moment. The current programming identity separates at least:

- instruction text;
- source text;
- derived source structure;
- derived instruction intent;
- console before the repair;
- failure outcome;
- corrected source or response;
- repair delta;
- console after the repair;
- resolution outcome;
- execution and platform context.

The binding/integration layer can then learn relations such as `language + requested behavior + failure signature + repair + successful outcome`, rather than treating an entire episode as one undifferentiated string.

### Give sparse semantic intent its own pool

**Experiment-supported, strongly.** The debug topology GA used pools `[1,2,3,5,6,10]` and achieved fitness `19.0547`, with exact `1.0`, held-out execution `1.0`, structural transfer `1.0`, and OOV honesty `0.6667`. Adding instruction-intent pool `12` produced fitness `20.6832`, exact `1.0`, held-out execution `1.0`, structural transfer `1.0`, and OOV honesty `1.0`.

The intent representation should encode combinations such as language, behavior, artifact type, and important constraints. Route on sufficiently specific combinations. Language-only or generic “project” evidence is too broad for safe composition in the current programming brain.

Sparse intent is class evidence, not a unique-answer key. Two valid artifacts can
share the same complete `language + behavior` atoms. When that happens, do not
let binding use count choose an answer: retain the raw character pool and use
its motif/context evidence to distinguish the competing targets. Reserve exact
feature recall for feature sets that resolve to one distinct target; richer
multi-behavior requests should continue through grounded composition.

**Proven live for polyglot composition.** Restricting component retrieval to complete `language + behavior` evidence produced `6/6` correct multi-language projects, `12/12` executable components, and `2/2` honest OOV responses on the persisted brain. The read-only bracket remained at tick `198824`.

**Proven live for paraphrase routing.** A ranked complete manifest supported by one language plus one concrete behavior must outrank a raw character-similarity fallback. After reinforcing six canonical language/second-power episodes, raw similarity became biased toward the recent square examples. Four unrelated enterprise paraphrases still produced the correct ranked manifest candidate, but a response-selection rule ignored a lone non-exact manifest and returned the square fallback. Selecting that grounded manifest fixed native, platform, and cross-language paraphrases while retaining polyglot composition and OOV honesty. This is an inference-policy lesson; do not “fix” it by memorizing every paraphrase.

### Keep long supervised action outputs out of concept promotion

**Proven live.** For source-code/response action pools, use `max_concept_member_count = 1` unless a new experiment demonstrates a safe alternative. Allowing response suffixes to undergo ordinary concept neurogenesis caused about 6.46 million terminals before reaching the midpoint of a 582-example Python curriculum, without improving recall.

The action remains atom-grounded and binding-addressable; this setting prevents redundant suffix concepts from consuming the brain.

### Model failure, repair, and success as separate causal signals

**Proven live.** Do not train only `instruction -> corrected code`. When execution data is available, present an episode containing the failing source, console output, environment, failure classification, repair delta, corrected source, new console output, and resolution. This lets the brain learn the transition from failure to repair to verified success.

Relevant environment features include language and version, operating system, architecture, dependency/toolchain versions, resource constraints, and command. Include a feature only when it can affect the observed behavior; irrelevant metadata increases collisions.

### Represent underspecification and OOV explicitly

**Experiment-supported, strongly.** The instruction-intent pool improved OOV honesty from `2/3` to `3/3` in the GA curriculum while preserving other scores. An unknown or underconstrained request should activate explicit evidence for insufficient specification rather than weakly matching the nearest familiar language or task.

False confident generation is worse than an honest unsupported response. Every curriculum gate therefore needs held-out OOV and underspecification cases.

### Configure for integration, not lookup alone

**Proven live.** Exact recall is necessary but insufficient. Every candidate brain configuration must also pass:

- paraphrases of trained instructions;
- held-out inputs that preserve behavior but change values or structure;
- structural transfer;
- cross-language transfer where appropriate;
- multi-component composition;
- OOV honesty;
- execution of generated artifacts;
- retention of earlier curricula.

A candidate that raises exact recall while lowering any protected integration or retention gate is not an improvement.

Integration must include experiential learning, not only safe retrieval. A coding brain should receive a previously unseen rule or API contract in the task context, attempt a solution, execute it, and co-activate the instruction, source, environment, console output, failure outcome, repair delta/relation, corrected source, successful console output, and verified resolution. Re-query both the experienced task and a held-out task that shares the abstract repair but changes identifiers and constants. Exact post-correction recall alone is memorization; improvement on the held-out task is the transfer evidence. Always re-run protected retention after admitting the experience.

The stronger target is causal multi-domain integration. Train disciplines independently, request a never-observed artifact whose requirements intersect them, execute the result, and then remove each premise one at a time. Every removal must suppress the corresponding intent feature and make the deliberately complete fixture fail; otherwise the benchmark only demonstrates retrieval or accidental overlap. Include mutually exclusive premises to prove that the active context can override an otherwise familiar policy. Measure traversal quality with correctness, execution, causal ablations, latency, activated-feature count, and retention—not with fluent surface form. After a verified success or repair, admit the full environment episode transactionally so later structurally similar tasks can reuse the relationship without erasing protected knowledge.

Do not keep validating integration against the same surface domain used to teach the fragments. A serious holdout changes the class name, domain objects, state transitions, identifiers, constants, method contract, and execution harness while retaining only the abstract discipline intersection. Passing the original synthesis but failing this domain-shifted holdout means the brain learned a useful composition motif but has not yet generalized the motif into a reusable program-construction procedure.

Grounded source motifs may expose explicitly declared output parameters without becoming a tokenizer or unconstrained generator. The response bytes still come from learned atom-level evidence; a fragment declares a named placeholder and its restricted parameter kind, while the decoder fills it only from a validated value explicitly present in the current prompt. Missing, malformed, undeclared, or unresolved parameters invalidate the composition. This separates invariant learned structure from task-specific symbols and lets the same wired motif instantiate a genuinely new class identity deterministically.

Measure multi-hop synthesis separately from near-neighbor transfer. Construct tasks whose solution was never present in the curriculum and whose indispensable premises are distributed across several learned disciplines. Require the brain to bind those premises into an executable artifact, not merely name the contributing topics. A strong gate should include: a runnable or mechanically checkable result; held-out changes to identifiers, values, and surface wording; premise ablations where removing one required fact prevents or changes the answer; contradiction tests where an updated premise propagates to the result; and a traversal budget that records inference latency and the number of activated concepts/pools. Passing exact recall or one-hop repair does not establish this capability. The target is correct, efficient traversal of a long compatible chain followed by novel composition and environmental verification.

## Known-good programming baseline

The persisted programming brain currently derives from `brains/coding_debug.identity.toml`. Treat these as a starting point for similar supervised coding brains, not universal constants.

| Setting | Current value | Evidence and rationale |
|---|---:|---|
| Binding threshold | `3` | Proven live across the retained curriculum. |
| Binding moment window | `256` | Proven live for the richer coding episode; the smaller coding identity used `128`. |
| Minimum atom score | `0.94` | Current known-good recall baseline. |
| Instruction-intent recent window | `256` | Supports sparse intent evidence over the full episode. |
| Instruction-intent max concept members | `8` | Current known-good compact semantic concepts. |
| Typical sensory concept threshold | `6` | GA favored `6` over `3` and narrowly over `5` in one search; replicate before generalizing. |
| Typical sensory decay | `0.00002` | Current live retention baseline. |
| Typical sensory prune threshold | `0.001` | Current live retention baseline; must remain guarded by old-curriculum tests. |
| Corrected-source/action recent window | `128` | Current known-good supervised output window. |
| Corrected-source/action max concept members | `1` | Prevents destructive response-suffix neurogenesis growth. |

## Genetic-search findings

| Experiment | Result | Interpretation |
|---|---|---|
| Concept threshold `3 -> 5 -> 6` | Fitness `18.8259 -> 19.0239 -> 19.0547` | **Experiment-supported.** Threshold `6` was best in this narrow debug search, but the difference from `5` was small. |
| Add instruction-intent pool `12` | Fitness `19.0547 -> 20.6832`; OOV `0.6667 -> 1.0` | **Experiment-supported, strongly.** Retain the intent pool for coding and test analogous semantic pools in other domains. |
| Joint-score thresholds `0.35`, `0.55`, `0.75` | Equal fitness `19.0547` | **Provisional.** These thresholds were behaviorally equivalent on the small curriculum. They require harder collision cases before selection. |
| Joint-score threshold `0.90` | Fitness fell to `16.4547`; held-out execution `0.6` | **Rejected for this workload.** It discarded useful combined evidence. |
| Reduce max concept members `24 -> 16` | Fitness `18.8259 -> 18.8457` | **Inconclusive.** Small gain; insufficient evidence for a default change. |
| Remove evidence pool `3` | Fitness remained `18.8259` on small gate | **Inconclusive.** The gate was not broad enough to prove that the pool is redundant. Do not remove it from the live brain without a full retention ablation. |

GA fitness is meaningful only when all essential gates are components of the objective. Include accuracy, execution, transfer, OOV honesty, retention, and resource costs. A fast but forgetful or confidently wrong brain must not win.

## Findings that failed or were revised

- **Rejected:** treating a single raw-text pool as sufficient for paraphrase, OOV, and cross-language routing. Atom grounding remains essential, but derived feature pools provide the discriminating evidence.
- **Rejected:** ordinary concept promotion for long response/source action streams. It produced severe terminal growth without recall benefit.
- **Rejected:** routing generated components from language evidence alone. It can retrieve the wrong behavior in that language.
- **Rejected:** generic artifact labels such as “project” as sufficient composition evidence. Prefer complete `language + behavior` or more specific feature sets.
- **Rejected:** counting a container label such as `ARTIFACT:PROJECT` as a second requested behavior. A single-language request with one concrete behavior still needs sensory/motif disambiguation when that sparse feature class has multiple learned artifacts.
- **Rejected:** returning the dependency-free prefix of a file after its requested behavioral fragment failed dependency closure. Quarantine that entire file and preserve only separate files whose dependency graphs settle completely.
- **Rejected:** allowing a recent raw character-similarity match to outrank a complete artifact supported by ranked `language + behavior` evidence. Recency can otherwise pull unrelated requests toward the newest same-language lesson.
- **Rejected:** allowing fuzzy raw-character fallback when a programming-language feature fired but no compatible code action was grounded. During MathInstruct training, an unseen strict-TypeScript physics-project request returned an unrelated construction-cost solution with confidence `1.0`. A recognized language feature is therefore a domain boundary: exact raw episodes remain authoritative, but unresolved programming intent must compose from compatible feature bindings or return honest OOV.
- **Rejected for the current debug workload:** `min_joint_score = 0.90`; it reduced held-out execution and structural transfer.
- **Operational warning:** compiler cache, first-run setup, permissions, or toolchain failures can look like inference failures. Benchmarks must use controlled environments and separately report model selection, compilation, execution, and environmental failure.

## Curriculum and retention protocol

1. Establish atom-level grounding and basic concepts with the toddler curriculum.
2. Add K-12 knowledge to supply broad context and relationships.
3. Train programming progressively from syntax and small functions through debugging, systems, polyglot composition, and enterprise projects.
4. After every training block, run read-only gates for all earlier stages. Verify that the brain tick does not change during evaluation.
5. Measure exact recall, paraphrase routing, execution, integration/transfer, OOV honesty, and resource growth independently.
6. Reject or revise any training/configuration change that improves the new domain by materially damaging protected knowledge.
7. Checkpoint only after the new block and all protected gates pass. Record the identity, snapshot, curriculum position, and benchmark outputs together.

For very large corpora, a “block” must be bounded rather than an entire corpus phase. The current programming curriculum uses guarded 16,384-row blocks. Every 32-row transaction is WAL-flushed before acknowledgement, and the full snapshot is replaced once at the block boundary. Each block then runs distributed corpus recall (including the first and newest trained rows), complete foundational retention, and the strict enterprise battery before the next block is permitted. This bounds WAL replay to one block while avoiding three redundant rewrites of an increasingly large snapshot. A checkpoint without a retention gate proves durability, not non-interference.

Bulk posting must preserve inference windows without throwing away throughput. Measure every batch transaction, retain the block-wide maximum plus an exponential moving average, and adapt the next block from the maximum observed lock rather than the final batch alone. A last-only measurement can hide an earlier stall when a faster final batch overwrites it. The current live ceiling is eight seconds; batch size `32` remains justified while the measured maximum stays below that ceiling.

Preserve the last accepted brain until the candidate chunk passes. For a mutable `.wbrain` container, the guard must be an independent atomic copy: a hard link shares the same inode and therefore shares in-place slot and appended-record mutations. Delete the guard only after corpus, foundation, and enterprise gates all pass; retain it and stop training on failure. Recovery still requires coordinated node shutdown, container restoration, WAL reset, and progress rewind—never replace the live server's brain underneath it. Hard-link guards remain suitable only for immutable checkpoint files that are replaced atomically rather than modified in place.

Read-only validation must distinguish learned topology from tier residency. In this architecture inference can page evicted neurons and their terminals from SSD into RAM. Consequently `resident_terminals`, `evicted_neurons`, and the current `total_terminals` alias may change during an inference-only battery even though no wiring was created or removed. Protect the tick and stable topology fields (`pool_count`, `total_neurons`, `total_concepts`, `total_binding`, and `binding_pool_id`); record residency changes as operational telemetry rather than treating them as learning. Snapshot/WAL identity remains the strongest durability-level evidence when a byte-level non-mutation claim is required.

Phone-class deployment is a design target, not a later compression exercise. When training and maintenance are idle, ordinary inference must complete in well under one second on phone-class storage and memory. Keep only compact symbolic routing state resident; let the input select a minimal focus, page individual neurons along the learned focus chain, expand a symbol into richer entities only when the chain requires it, emit the response or action sequence, and return that scope to sleep. A compact concept may be represented by a single byte or similarly small identifier as long as atom-grounded expansion remains deterministic and available on demand. Every production admission must therefore measure warm and cold latency, bytes read, neuron bodies awakened, peak resident bytes, pools traversed, focus-chain depth, output correctness, and post-inference return-to-sleep. A desktop result that is correct only after broad hydration does not satisfy the architecture.

Until the node exposes request-scoped SSD-byte and focus-depth counters, use
resident-terminal delta as the explicit scope proxy and record the telemetry
gap rather than implying those dimensions were measured. The executable
desktop gate sleeps before every cold request, repeats it warm, requires
correct deterministic output, enforces `cold <= 1.0s`, `warm <= 0.5s`, caps
resident scope at ten percent, and requires zero residency after every trial.
Passing is necessary but does not replace the same gate on phone-class
hardware.

“Pre-training” in Wizard Vision should mean accelerated construction of complete atom-grounded episodes and their derived simultaneous features. It must not mean replacing character/byte learning with an external token vocabulary.

## Procedure for configuring a new domain brain

1. Define the raw sensory and action streams.
2. Identify independent, causally useful feature families and assign each to a pool.
3. Define the integration evidence required for a confident action; avoid broad single-feature routing.
4. Add explicit unknown, ambiguity, failure, and success signals.
5. Select a small protected curriculum containing exact, paraphrase, transfer, OOV, and outcome cases.
6. Establish conservative concept, decay, and pruning settings from the closest proven identity.
7. Run ablations before adding complexity: each derived pool must demonstrate value on held-out behavior.
8. Search thresholds/topology genetically only with a multi-objective fitness that protects retention and resource usage.
9. Expand the curriculum in stages, checkpointing only passing configurations.
10. Record each conclusion below with its artifacts and evidence label.

Use parallel disposable micro-brains before loading a multi-gigabyte persisted
brain for an architecture or configuration experiment. Give every variant the
same identity and native decoder, train only the target motif plus deliberately
selected interference motifs, and run execution, causal ablation, OOV, and
ordering gates. Repetition sweeps and noise-order sweeps can then complete in
seconds. Only a micro-scale winner should pay the production-brain load and
full-retention cost. Micro-brains prove algorithmic behavior, not behavior at
scale; the production admission remains mandatory.

Before admitting any structured lesson, verify that its prompt actually fires
the intended behavioral feature. A prompt with only language/domain atoms is
too broad even when its English wording sounds specific. Repetition cannot
repair a missing feature edge.

For a new programming brain, use `scripts/train_programming_brain.py` rather than reconstructing the successful training order manually. The exact stage order, repetitions, transaction guards, resume behavior, and gate artifacts are described in `docs/PROGRAMMING_BRAIN_REPRODUCTION.md`.

## Evidence ledger

| Date | Finding | Evidence | Artifacts |
|---|---|---|---|
| 2026-07-13 | Concept threshold `6` narrowly led the tested thresholds. | Experiment-supported | `runtime/debug_topology_ga-20260713/` |
| 2026-07-13 | Instruction-intent pool completed OOV honesty without reducing exact, execution, or transfer. | Experiment-supported, strongly | `runtime/debug_intent_pool-20260713/` |
| 2026-07-13 | Joint threshold `0.90` was too restrictive for the debug curriculum. | Rejected for this workload | `runtime/debug_joint_sweep-20260713/` |
| 2026-07-13 | Long action-stream concept promotion caused extreme terminal growth without recall gain. | Proven live observation | `brains/coding_small.identity.toml` |
| 2026-07-15 | The persisted programming brain passed protected toddler, K-12, OOV, Python execution, debug exact/held-out/structural/OOV gates without changing tick. | Proven live | `runtime/brains/programming-integrated-20260713/` and benchmark reports in `runtime/` |
| 2026-07-15 | Complete `language + behavior` evidence safely composed polyglot manifests: projects `6/6`, components `12/12`, OOV `2/2`, tick unchanged. | Proven live | `runtime/programming-polyglot-composition-after-routing.json` |
| 2026-07-15 | Six canonical multilingual episodes generalized to all six unseen paraphrases after 36 presentations, adding only six bindings. | Proven live | `runtime/benchmarks/multilanguage-after-intent-reinforcement.json` |
| 2026-07-15 | A single ranked complete manifest must outrank raw similarity for one-language requests. An initially green semantic-stress diagnostic was discovered to return success despite missing recalls; after making that gate strict and sweeping equivalent coding verbs and enterprise behavior phrases, all `11/11` suites genuinely passed with no tick mutation. | Proven live | `runtime/benchmarks/enterprise-retention-strict-semantic.json` |
| 2026-07-15 | Post-correction foundation retention remained toddler `32/32`, K-12 `16/16`, OOV `3/3`, Python execution `10/10`, and all debug transfer gates perfect. | Proven live | `runtime/benchmarks/integrated-retention-after-ranked-manifest.json` |
| 2026-07-15 | Semantic feature coverage must be tested across equivalent action verbs and relational phrases, not only canonical keywords. Adding “develop” and behavior-equivalent authorization, replay, rollback, correlation, and redaction evidence raised strict semantic stress from trained `2/4`, held-out `1/4` to `4/4` and `4/4` without training held-out answers. | Proven live | `runtime/benchmarks/semantic-stress-strict-after-feature-sweep.json` |
| 2026-07-15 | Multi-million-row phases require bounded train/checkpoint/gate cycles. The supervisor now limits direct pretraining to 4,096 durable rows, then blocks continuation on distributed corpus recall plus complete foundational and strict enterprise retention. | Operationally enforced | `scripts/programming_curriculum_supervisor.py` |
| 2026-07-15 | The first bounded MathInstruct cycle reached durable row `188720`, recalled `32/32` distributed corpus samples, retained every foundation/debug/Python gate, and passed strict enterprise `11/11` with tick `207532 -> 207532`. | Proven live | `runtime/brains/programming-integrated-20260713/mathinstruct-domain-safe.row-188720.retention-gate.json` |
| 2026-07-15 | A second guarded MathInstruct cycle reached durable row `192816`, recalled `32/32`, retained every foundation gate, and passed strict enterprise `11/11` with tick `211628 -> 211628`. Two consecutive clean calibration cycles justified widening retention blocks to `16384` rows. | Proven live | `runtime/brains/programming-integrated-20260713/mathinstruct-domain-safe.row-192816.retention-gate.json` |
| 2026-07-15 | For the then-authoritative atomically replaced `brain.bin`, retain a hard-linked last-known-good snapshot across each candidate chunk and delete it only after gate acceptance. This guard mechanism is superseded by an independent copy when the authoritative representation is mutable `.wbrain`. | Historical operational finding, storage-specific | `runtime/brains/programming-integrated-20260713/brain/brain.last-good.json` |
| 2026-07-15 | A third guarded MathInstruct cycle reached durable row `196912`, recalled `32/32`, retained toddler `32/32`, K-12 `16/16`, OOV `3/3`, Python execution `10/10`, all debug transfer gates, and strict enterprise `11/11` with tick `215724 -> 215724`. | Proven live | `runtime/brains/programming-integrated-20260713/mathinstruct-domain-safe.row-196912.retention-gate.json` |
| 2026-07-15 | Batch size `32` later reached a `10.3935`-second live posting lock, exceeding the configured `8`-second ceiling even though the final batch took only `5.8143` seconds. Tune from the maximum, not the last sample; the responsive controller scales the next block to `24` (`floor(32*8/10.3935)`). | Proven live calibration | `runtime/brains/programming-integrated-20260713/mathinstruct-domain-safe.progress.json` |
| 2026-07-15 | Inference-only enterprise evaluation paged cold neurons from SSD, changing resident terminal and eviction counters while tick and stable topology counts remained fixed. Residency is not a valid structural non-mutation invariant. | Architecture-confirmed live observation | `crates/brain/src/pool.rs`, `crates/brain/src/brain.rs`, and row `196912` runtime stats |
| 2026-07-15 | At row `201008`, 4,096 MathInstruct episodes grew the full snapshot by about `121.6 MB`. With WAL acknowledgement already making every batch durable, snapshotting four times per guarded block creates increasing full-snapshot write amplification without improving retention admission. Align full snapshots with the `16,384`-row guard boundary. | Proven live operational measurement | `runtime/brains/programming-integrated-20260713/brain/brain.bin` and `mathinstruct-domain-safe.progress.json` |
| 2026-07-15 | An unseen strict-TypeScript Multiscale Robot World prompt incorrectly selected recent MathInstruct prose despite detecting `LANGUAGE:TYPESCRIPT` and finding zero compatible feature candidates. Programming-language intent now inhibits ungrounded raw fallback, and capstone safety is a required enterprise suite that accepts only a structurally grounded manifest or honest OOV. | Failure reproduced; correction unit-tested, pending live deployment gate | `runtime/benchmarks/capstone-readiness-before-domain-guard.json`, `scripts/programming_capstone_readiness.py` |
| 2026-07-15 | Experiential coding must be measured as `baseline attempt -> verified failure/success episode -> experienced retry -> held-out structural transfer`, with the held-out function, constants, and wording changed. The live brain initially gave honest OOV for the unseen Nebula rule, learned the verified `- -> +` repair in six atom-grounded episodes, executed the corrected Nebula function, and transferred the relation to an unseen Quasar function with different identifiers, factor, and offset. Foundation and enterprise retention both passed, tick advanced exactly `6`, the admitted state was checkpointed, and the rollback link was released only afterward. | Proven live | `runtime/benchmarks/experiential-generalization-live.json` |
| 2026-07-15 | Cross-disciplinary integration requires a dedicated multi-hop synthesis gate: the final artifact must be absent from training, necessary premises must be distributed across learned domains, and execution, held-out variants, premise ablations, contradictions, and traversal cost must all be measured. | Acceptance protocol defined; benchmark implementation pending | This guide, “Configure for integration, not lookup alone” |
| 2026-07-15 | A language-intent boundary must inspect candidate compatibility rather than suppress all fuzzy/ranked sensory recall. Allow a single-language ranked source only when language+behavior features ground it and its bytes are shaped like that language; continue to reject cross-domain prose and partial polyglot answers. This restored Python paraphrase execution from `4/5` to `5/5` while the unseen TypeScript capstone remained honest OOV. | Proven live | `runtime/benchmarks/capstone-readiness-ranked-source-guard.json` and row `213296` retention artifacts |
| 2026-07-15 | Retention orchestration must validate the authoritative full report, not a compact stdout summary, and the integrated benchmark itself must exit nonzero when any protected foundation, syntax, execution, or transfer count is incomplete. Row `213296` then passed corpus recall `32/32`, integrated retention (including Python `10/10`), and enterprise `12/12` with stable structure and tick. | Proven live and operationally enforced | `runtime/brains/programming-integrated-20260713/mathinstruct-domain-safe.row-213296.retention-gate.json` |
| 2026-07-15 | A 12-discipline synthesis baseline activated 15 intent labels and retrieved 13 ranked candidates but did not produce the required new class; its contradictory no-retry premise also failed to propagate. This separates broad candidate activation from successful causal composition and establishes the next routing/topology bottleneck. | Proven live baseline; capability not yet admitted | `runtime/benchmarks/multidomain-synthesis-baseline.json` |
| 2026-07-15 | Concurrent multi-hop inference exposed a `15.4753`-second training transaction at batch `24`. Preserve maximum timing telemetry across worker restarts, adapt batch size downward within a running worker, and pass the live-lock ceiling into every corpus invocation. The resumed worker selected batch `12` and returned to roughly `1–3` second transactions. | Proven live and operationally enforced | `mathinstruct-domain-safe.progress.json` and `tools/training_standard/drive_corpora_brain.py` |
| 2026-07-15 | A retention block target must be immutable across process failure. Derive it from the accepted guard's starting row rather than `current row + gate size`; otherwise each restart silently extends the ungated interval. The active block remained fixed at row `229680` after restarting from row `217796`. | Proven live and operationally enforced | `brain.last-good.json` and `curriculum-supervisor.status.json` |
| 2026-07-15 | Multi-domain synthesis requires structured fragments to survive language+behavior subset recovery; complete-manifest-only filtering left learned fragments unreachable on large prompts. Admit only validated relative-path fragments with nonempty roles, sources, and dependencies. A complete manifest must still outrank an isolated exact fragment when their sparse features collide. | Proven live core routing fix | `crates/node/src/brain_api.rs` |
| 2026-07-15 | The programming brain composed a never-trained `AdaptiveCoordinator` from twelve independently learned disciplines: validation, authorization, migration, recursive redaction, correlated logging, circuit breaking, async retry, idempotency, optimistic concurrency, deduplication, atomic transaction, and transactional outbox. The artifact executed; removing each premise removed its feature and broke execution; a contradictory no-retry premise selected a mutually exclusive single-attempt policy and executed correctly. After resolving partial-fragment shadowing, semantic stress returned to exact/held-out `4/4`, enterprise passed `12/12`, structure and tick stayed stable, and the 102-episode candidate was checkpointed at tick `248636`. | Proven live and admitted | `runtime/benchmarks/multidomain-synthesis-after-precedence.json`, `semantic-stress-after-fragment-precedence.json`, and `multidomain-enterprise-after-precedence.json` |
| 2026-07-15 | The reproducible trainer now runs verified failure-to-repair transfer and causal 12-discipline synthesis after the guarded corpus curriculum, each with transactional rollback, foundation retention, enterprise retention, and checkpoint admission. Corpus transaction-size calibration also carries across phase boundaries so a new subject cannot repeat a lock duration already proven unsafe in an earlier subject. | Implemented and contract-tested | `scripts/train_programming_brain.py`, `scripts/programming_curriculum_supervisor.py`, and `tests/test_programming_runtime_contract.py` |
| 2026-07-15 | A domain-shifted twelve-discipline holdout now requests a never-trained `ResilientFulfillmentService`, replacing the account-transfer class, inventory/state contract, role model, identifiers, constants, and execution harness. Its optional causal sweep removes each abstract discipline independently. This is the next admission gate for distinguishing reusable integration from replay of the admitted coordinator motif. | Implemented; live result pending completion of the guarded corpus block | `scripts/programming_multidomain_holdout.py` |
| 2026-07-15 | The first non-mutating domain-shifted baseline failed in `4.5755s`: it emitted two isolated authorization/logging functions rather than the requested class. Diagnostics showed that valid domain paraphrases activated only part of the twelve-discipline set. The intent encoder now recognizes the missing order-validation, resettable-circuit, order-key-replay, expected-inventory-version, duplicate-SKU-work, and inventory-rollback relations as their existing abstract features. All 30 intent tests pass; the updated binary and full holdout remain to be evaluated after the guarded corpus block. | Baseline proven; feature-path correction implemented but not yet deployed | `runtime/benchmarks/multidomain-holdout-during-mathinstruct.json`, `crates/brain/src/pool.rs`, and `crates/brain/tests/instruction_intent_encoding.rs` |
| 2026-07-15 | After deploying the intent-generalization build, the domain-shifted holdout activated all twelve required abstract disciplines (16 total labels including language/container signals) and retrieved 3,476 characters across four relevant learned components, up from 982 characters and two components. It still failed because `ResilientFulfillmentService` was never constructed; execution now reports that exact missing class rather than a harness-import artifact. This localizes the next bottleneck after successful intent traversal: grounded fragments are concatenated as domain-specific artifacts, but there is no learned parameterized structural motif that binds a novel class/state/method contract across them. | Proven live; composition generalization not yet admitted | `runtime/benchmarks/multidomain-holdout-after-intent-generalization-v2.json` |
| 2026-07-15 | The completed MathInstruct phase passed its authoritative gate on the updated node at tick `264279`: corpus recall accepted `64/64` samples (`59` exact), toddler `32/32`, K–12 `16/16`, Python trained/paraphrase execution `5/5` each, TypeScript trained/paraphrase execution `3/3` each, OOV honesty complete, and enterprise `12/12` with stable tick and topology. MetaMath began from row zero under a new last-good guard using batch `12`, proving that live-lock calibration carried across the phase boundary. | Proven live and admitted | `runtime/brains/programming-integrated-20260713/mathinstruct-domain-safe.completion-gate.json` and `metamathqa-domain-safe.progress.json` |
| 2026-07-15 | The grounded-fragment protocol now supports explicitly declared `python_class_named` and `python_method_named` parameters. A learned source motif containing `{{CLASS_NAME}}` or `{{METHOD_NAME}}` can bind a valid identifier from “class/method named/called …” in the live prompt; missing cues, invalid identifiers, unknown parameter kinds, and unresolved placeholders reject the entire composition. Existing fragments remain byte-identical and all API composition/routing plus 30 runtime-contract tests pass. These are the first structural parameterization primitives required by the domain-shifted holdout; state-contract motifs remain to be learned and generalized. | Implemented and contract-tested; deployment pending next guarded boundary | `crates/node/src/brain_api.rs` |
| 2026-07-15 | The next guarded experience experiment is encoded as thirteen independently trainable fragments spanning the twelve fulfillment disciplines and structural commit closure. No training response contains a complete service. The executable fixture proves that the same atom-grounded motif can instantiate both `ResilientFulfillmentService.fulfill` and the never-trained `DurableWarehouseEngine.allocate_order`; training is transactionally protected and cannot run beside a corpus worker, and admission requires both executions plus foundation and enterprise retention. This tests symbol binding and structural composition without confusing it with whole-artifact recall. | Fixture and transaction gate contract-tested; live training pending MetaMath boundary | `scripts/programming_parameterized_fulfillment.py` and `tests/test_programming_runtime_contract.py` |
| 2026-07-15 | The pre-training two-symbol baseline failed causally: both requested interfaces activated all twelve disciplines and returned the same 3,476-byte set of separate learned components, but neither requested class existed. The read-only run overlapped MetaMath, so its tick delta is explicitly attributed to recorded concurrent training PIDs rather than to inference. This provides the before-state for the guarded parameterized-motif admission. | Proven live baseline | `runtime/benchmarks/parameterized-fulfillment-baseline.json` |
| 2026-07-15 | A third-domain transfer holdout now requests `ResilientJobScheduler.schedule` with scheduler capacity, job/slot commands, operator authorization, dispatch retry, capacity rollback, and a job-scheduled outbox while retaining the same twelve abstract disciplines. It is intentionally withheld from fulfillment-motif training. Passing the two fulfillment symbol variants but failing this scheduler proves symbol/structure reuse without state-contract transfer; passing both is evidence that the integrated motif generalized across business state models. | Implemented and contract-tested; live result pending parameterized-motif admission | `scripts/programming_domain_transfer_holdout.py` |
| 2026-07-15 | MetaMath's first 16,384-row gate initially reported only `14/32` corpus-local accepted answers even though all prompts returned in milliseconds. A failed sample returned the exact previously admitted MathInstruct explanation and the same correct final value (`sqrt(5)`), while MetaMath held fourteen alternative wordings for that identical prompt. Recall admission now accepts any exact supervised response from every corpus with a positive durable ledger, never arbitrary output. The same sample set then passed `32/32`, preserving deterministic one-answer recall without falsely treating retained cross-corpus knowledge as interference. Gate-command errors also retain stdout diagnostics. | Proven live and corrected | `scripts/programming_corpus_recall.py`, `scripts/programming_curriculum_supervisor.py`, and `tests/test_programming_runtime_contract.py` |
| 2026-07-15 | Six parallel micro-brains reproduced the parameterized-composition failure regardless of `1x`, `3x`, or `6x` presentation and regardless of interference order. All twelve dependent fragments fired, but the root fragment carried `STRUCTURE:SERVICE_CLASS` while “class named …” did not; unifying those equivalent structural cues restored all 13 fragments. | Causal micro-brain sweep | `runtime/benchmarks/microbrain-composition-service-class.json` |
| 2026-07-15 | A partial unrelated historical dependency chain globally vetoed a complete target chain. Pruning incomplete connected components to a dependency-closed subgraph preserved the complete parameterized artifact under every noise order, including heavy prior noise. | Causal micro-brain sweep and unit-tested core fix | `runtime/benchmarks/microbrain-composition-closed-components.json` and `crates/node/src/brain_api.rs` |
| 2026-07-15 | Component recovery must use language plus a behavioral namespace, never language plus domain/container/structure. The transaction lesson also failed to emit its intended atomic-transaction feature despite seemingly specific wording. After restricting recovery and preflighting lesson features, all six variants executed both unseen interfaces (`2/2`) and rejected all twelve premise ablations (`12/12`); one presentation matched six. The unrelated scheduler remained honest OOV, localizing the next problem to state-contract transfer. | Causal parallel micro-brain sweep | `runtime/benchmarks/microbrain-composition-causal-complete.json` and `scripts/programming_microbrain_lab.py` |
| 2026-07-15 | Training each fragment under both domain-rich and abstract behavioral contexts, while retaining the same atom-grounded response, makes the abstract relation reachable without discarding contextual specificity. Controlled prompt-bound parameters for class/method, resource shape, state/item/amount fields, role, result, event, and log key then instantiated the learned 13-fragment motif in two unseen inventory interfaces, a withheld scheduler/capacity model, and a third never-trained quota/credits model. All six repetition/interference variants passed every execution and all `12/12` causal ablations; `1x` matched `6x`. | Causal parallel micro-brain sweep and parameter-safety tests | `runtime/benchmarks/microbrain-generic-state-contract-causal.json`, `scripts/programming_state_contract_holdout.py`, and `crates/node/src/brain_api.rs` |
| 2026-07-15 | The one-shot generalized motif was admitted to the full programming brain as 26 domain-rich/abstract episodes (`tick 280663 -> 280689`). An initial enterprise run exposed a scale-only collision: a valid historical fragment subgraph shadowed retained multi-file manifests on explicit project prompts. Explicit `ARTIFACT:PROJECT` now prefers complete manifest composition, restoring cross-project `6/6` and OOV `2/2`. The corrected state passed foundation (toddler `32/32`, K–12 `16/16`, Python `5/5 + 5/5`, debug `6/6 + 6/6 + 4/4`, OOV complete), all enterprise suites `12/12`, both inventory symbols, scheduler transfer, and quota transfer. Tick/topology stayed fixed during gates; the snapshot was checkpointed and its rollback guard released. | Proven live and admitted | `runtime/benchmarks/parameterized-state-contract-post-fix.json`, `parameterized-foundation-project-priority.json`, `parameterized-enterprise-project-priority.json`, and `cross-project-project-priority.json` |
| 2026-07-15 | After admitting the one-shot generalized state-contract motif, the next guarded MetaMath block reached durable row `32768` with all `16384/16384` posts accepted and zero failures. Its authoritative gate accepted distributed corpus recall `32/32`, retained toddler `32/32`, K–12 `16/16`, Python trained/paraphrase execution `5/5 + 5/5`, debug exact/held-out/structural `6/6 + 6/6 + 4/4`, OOV honesty `3/3`, and strict enterprise `12/12`; tick stayed `297073` and topology was unchanged. This is direct scale evidence that one-shot motif admission did not destabilize broad mathematical pretraining or protected coding integration. The accepted snapshot became the new row-`32768` guard before training continued. | Proven live and admitted | `runtime/brains/programming-integrated-20260713/metamathqa-domain-safe.row-32768.retention-gate.json` |
| 2026-07-15 | Stopping every `16384` rows for a multi-gigabyte snapshot and the entire enterprise battery is safe but does not scale to multi-million-row curricula. Use two admission cadences: fast recall/foundation/code canaries every `16384` rows while the worker continues, and an immutable snapshot plus comprehensive enterprise gate every `131072` rows. Append every canary outcome to `curriculum-health.jsonl`; on failure terminate the candidate worker immediately and preserve the last accepted hard-linked snapshot, so failures are observable but cannot compound into later admitted state. Transport and lock windows are separate: send up to `256` ordered episodes per HTTP/WAL acknowledgement while releasing the brain mutex every calibrated `12` episodes. This preserves episode order, one binding/tick per episode, and WAL durability while removing repeated HTTP, base64, flush, and artificial-yield overhead. | Implemented and contract-checked; live throughput calibration pending deployment at an accepted boundary | `scripts/programming_curriculum_supervisor.py`, `tools/training_standard/drive_corpora_brain.py`, and `crates/node/src/brain_api.rs` |
| 2026-07-15 | The executable corpus plan contains `6,738,759` rows, not the stale `15,159,709` estimate produced by mismatched historical phase names and obsolete generated-corpus sizes. At durable row `49152` of MetaMath, `6,444,284` configured rows remain. Always derive totals from the live supervisor's `Phase(...)` definitions and matching progress filenames. | Accounting correction | `scripts/programming_curriculum_supervisor.py` and `runtime/brains/programming-integrated-20260713/*.progress.json` |
| 2026-07-15 | The row-`49152` MetaMath gate stopped on Python enterprise `11/12`: the secret-redaction paraphrase returned a non-executable unrelated answer once, while tick/topology remained fixed. Two immediate focused reruns returned the exact executable function and passed `5/5`, localizing this as transient first-query/routing state rather than knowledge loss. Failure artifacts must retain the wrong reply, decoder, and intent diagnostics; do not erase a transient failure by reporting only a later green rerun. | Failure quarantined; determinism diagnosis in progress | `runtime/brains/programming-integrated-20260713/metamathqa-domain-safe.row-49152.enterprise.json` and `runtime/benchmarks/python-enterprise-row49152-retry*.json` |
| 2026-07-15 | The row-`49152` first-query failure was reproduced causally with a disposable executable: a standalone redaction function and a correlated-logging module shared `LANGUAGE:PYTHON + ENTERPRISE:SECRET_REDACTION`, so exact-feature recall selected the historically more-used module. Exact feature readout now rejects conflicting targets, and single-behavior routing uses raw character motifs to supply the missing evidence. The executable micro-brain then selected `redact_secrets`; unique exact targets, all seven feature-composition tests, and 35 runtime contracts remained green. | Causal micro-brain reproduction and executable correction; production deployment pending guarded boundary | `crates/brain/tests/exact_feature_ambiguity.rs`, `crates/brain/src/brain.rs`, and `crates/node/src/brain_api.rs` |
| 2026-07-15 | The first non-halting corpus canary ran at durable MetaMath row `65536` while the worker remained alive. Recall passed `8/8`, toddler `32/32`, K–12 `16/16`, OOV `3/3`, and Python trained/novel syntax+execution `5/5 + 5/5`; the worker advanced to row `66048` without a stop. | Proven live | `runtime/brains/programming-integrated-20260713/curriculum-health.jsonl` |
| 2026-07-15 | Stopping a worker on canary failure is insufficient quarantine if a later supervisor can resume from the rejected progress/WAL offset. Persist a fail-closed quarantine containing the candidate row, durable offset, error, and last-good metadata; refuse every later attach or training start until the guarded snapshot and progress ledger are explicitly restored. | Contract-tested operational invariant | `scripts/programming_curriculum_supervisor.py` and `tests/test_programming_runtime_contract.py` |
| 2026-07-15 | Canary rollback must be one transaction with the brain server stopped: restore the recorded authoritative guard (`.wbrain` copy or legacy `.bin` link), delete the rejected WAL, rewind both RAM and durable progress to the guarded row, preserve timing calibration, then clear guard metadata and quarantine. `--restore-canary-quarantine` implements this sequence and refuses a live node. | Destructive-state simulation passed; later extended to `.wbrain` | `scripts/programming_curriculum_supervisor.py` and `tests/test_programming_runtime_contract.py` |
| 2026-07-15 | A non-halting canary cannot require zero tick/topology delta because the corpus worker intentionally continues learning beside it. Persist stats and RAM/durable rows before and after the canary, attribute the deltas to concurrent training, and reserve strict tick/topology non-mutation checks for the stopped-worker admission gate. | Contract-tested telemetry rule | `scripts/programming_curriculum_supervisor.py` and `tests/test_programming_runtime_contract.py` |
| 2026-07-15 | A failed 16,384-row canary should quarantine only the range after the latest passing canary, while the immutable full-gate snapshot remains the rollback authority. Persist `suspect_start_row..suspect_end_row` in the append-only health ledger before rollback clears the active marker. Replay the earlier green prefix and focus micro-brain bisection on that permanent suspect range; do not repeatedly diagnose the entire 131,072-row admission block. | Contract-tested triage rule | `scripts/programming_curriculum_supervisor.py` and `tests/test_programming_runtime_contract.py` |
| 2026-07-16 | At roughly `20.8M` resident terminals, live `/tick_profile` attributed `95.38%` of cumulative tick time to housekeeping (`87,995 us/tick` mean). The production node had inherited the eager default, which scans every terminal each tick. Corpus nodes must explicitly set `W1Z4RD_TICK_HOUSEKEEPING=lazy`; lazy decay applies the identical compounded decay when a neuron is touched without the O(total-terminals) sweep. Also set `W1Z4RD_DEFER_PROMOTION=1` to avoid repeated intermediate graph maintenance. Activate this only at an exact durable checkpoint, then compare pre/post tick profiles and retention gates before admitting the configuration. | Live bottleneck localized; configuration and safe-boundary activation implemented, post-restart measurement pending | `/tick_profile`, `scripts/programming_curriculum_supervisor.py`, and `runtime/brains/programming-integrated-20260713/` |
| 2026-07-16 | An attached legacy worker needs the same live canary loop as a supervisor-launched worker. At MetaMath row `98304`, the attached-worker canary passed corpus recall `8/8`, toddler `32/32`, K–12 `16/16`, OOV `3/3`, and trained/novel Python syntax plus execution `5/5 + 5/5`. Training advanced concurrently from row `98304` to `98560`; the report correctly attributed tick `+400`, bindings `+400`, concepts `+402`, and terminals `+23997` to that worker instead of treating them as inference mutation. | Proven live | `runtime/brains/programming-integrated-20260713/curriculum-health.jsonl` and `scripts/programming_curriculum_supervisor.py` |
| 2026-07-16 | At the completed MetaMath boundary (`180224` rows), a sparse `LANGUAGE:PYTHON + COMPARISON:LESS_THAN_ZERO` class selected a historical `is_prime` action for an unnamed “below zero” paraphrase even though trained recall and deeper structural-transfer tests remained perfect. Directly training the verified failure prompt and successful `is_negative` action for four atom-grounded episodes created an exact raw pathway; the correction survived restart and restored novel execution to `5/5`. Broad feature pools identify behavior classes but cannot resolve every artifact collision. When a verified failure exposes ambiguity, preserve the sparse features and add the complete sensory episode plus outcome instead of inventing a narrower tokenizer or globally increasing repetition. | Proven live experiential correction | `runtime/brains/programming-integrated-20260713/metamathqa-domain-safe.row-180224.foundation.json` and tick `444529 -> 444533` |
| 2026-07-16 | The same scale gate exposed four related enterprise paraphrase collisions: Python retry selected TypeScript retry, recursive redaction selected the observability project, an idempotent API selected an incomplete project, and default-deny authorization omitted its required file. Correcting all four failed prompts together with their verified complete artifacts raised the three affected suites from `3/5`, `3/4`, and `3/4` paraphrase execution to `5/5`, `4/4`, and `4/4`. Diagnose feature-class failures as a family: map every prompt-to-feature-to-candidate path and repair all equivalent collisions in one experiential sweep. | Proven live; full cross-suite admission gate running | `enterprise.corrected.json`, `platform.corrected.json`, and `project.corrected.json` under the active runtime |
| 2026-07-16 | A 13.65 GB checkpoint completed its atomic `brain.bin` replacement and WAL truncation but then held the brain mutex indefinitely on Windows during the redundant `sync_all()` after truncation. The snapshot and live corrections were durable, yet every brain-locked endpoint stalled. Removing that post-truncate full-file sync preserved the durability ordering—snapshot fsync and atomic replacement occur first, and the WAL is flushed before truncation—and allowed two production-scale checkpoints to return normally with the WAL compacted to its 8-byte header. Treat HTTP completion and immediate post-checkpoint inference as part of the checkpoint gate; file replacement alone is insufficient. | Causal production fix, unit-tested and verified twice at full scale | `crates/brain/src/store/wal.rs`, `crates/brain/tests/wal_wiring.rs`, and the active `brain.bin`/`brain.wal` |
| 2026-07-16 | Restoring a monolithic multi-gigabyte `brain.bin` before attaching cold tiers contradicts the intended sleep lifecycle and can exhaust the host before paging begins. A `.wbrain` container now keeps neuron bodies as individually addressed records, persists compact pool/brain routing metadata, reopens a synthetic whole brain with every neuron body asleep, and pages one requested id without retaining a duplicate store copy. Expensive Rust validation must run through `scripts/run_cargo_bounded.ps1` with one build job, below-normal priority, and an 8 GB available-memory floor; a hard system lockup occurred during the previous unrestricted development cycle. Do not restart or migrate the production brain until bounded direct restore and one-time migration are both proven on a small fixture. | Architecture correction proven on a bounded synthetic brain; production migration pending | `crates/brain/src/store/container.rs`, `crates/brain/src/store/wbrain_store.rs`, `crates/brain/src/wbrain_metadata.rs`, and `scripts/run_cargo_bounded.ps1` |
| 2026-07-17 | A legacy checkpoint can be converted without hydrating the full brain: parse its positional bincode stream one neuron at a time, overlay authoritative legacy cold-tier records, and append each neuron directly to `.wbrain`. Binding sequence, feature-atom, and character-motif indexes are derived state absent from the legacy format, so rebuild them one binding tree at a time and return every paged tree to sleep before advancing. A fixture proved source preservation, cold terminal restoration, zero-resident reopen, rebuilt exact routing, and `hello -> world` recall after a second reopen. Keep an 8 GiB host reserve during conversion and never infer migration safety from checkpoint size plus free RAM alone. | Architecture correction and bounded migration path proven on synthetic fixtures; production-scale validation pending | `crates/brain/src/streaming_migration.rs`, `crates/brain/src/brain.rs`, `crates/brain/src/store/wbrain_store.rs`, and `crates/node/src/brain_api.rs` |
| 2026-07-17 | The first production streaming attempt exposed two remaining full-pool duplications after neuron payloads were already bounded: every concept label was copied into the resident `.wbrain` lookup map, and the legacy snapshot's complete `label_to_id` map was deserialized before being discarded. At 9.72 GiB written these duplicates raised migration memory to 7.60 GiB and reduced host availability below 4 GiB, so the candidate was stopped before lockup and its partial destination removed. Consume the legacy map entry-by-entry; index atom labels for neurogenesis and binding labels for binding idempotence, but keep ordinary concept labels solely in their neuron records and restore them on page-in. Synthetic tests prove cold concept labels return on demand and binding retraining remains idempotent. | Causal production-scale failure localized and corrected; retry pending | `crates/brain/src/streaming_migration.rs`, `crates/brain/src/store/wbrain_store.rs`, `crates/brain/src/pool.rs`, and the first production migration telemetry |
| 2026-07-17 | Dropping one deserialized legacy label `String` at a time is not allocation-bounded on Windows: the allocator retained the released string pages, so the second production candidate still climbed from 3.83 to 6.47 GiB while output remained fixed at 9.72 GiB. Skip each bincode string's declared byte range directly into an I/O sink instead of allocating a temporary `String`. This preserves exact stream position and scalar IDs while keeping memory independent of historical label sizes. | Causal production-scale allocator behavior; zero-allocation parser implemented and fixture-tested | `crates/brain/src/streaming_migration.rs` and second production migration telemetry |
| 2026-07-17 | A zero-allocation discard is not sufficient if a long parser phase has no resource checkpoints. During the third production candidate, output again paused at 9.72 GiB while the migration working set rose from 3.51 to 5.30 GiB and host availability fell to 2.67 GiB. The process was stopped before lockup and available RAM immediately recovered to 9.80 GiB. Long discard/read phases must use a fixed buffer, check the host floor by bytes processed rather than records processed, and evict unused Windows working-set pages before measuring the floor. | Causal production-scale failure; bounded discard and in-phase circuit breaker implemented and fixture-tested | `crates/brain/src/streaming_migration.rs` and third production migration telemetry |
| 2026-07-17 | A resource-bounded migration must also be restart-bounded. Publish a crash-safe partial `.wbrain` manifest after each complete pool with the exact legacy byte offset, checkpoint identity, and completed-pool count. On restart, seek directly to the next pool; neuron records written after the last published manifest are an unreachable tail and can be safely superseded. Keep a separate durable stage marker so completed raw conversion also survives interruption during derived-index reconstruction. A multi-pool fixture resumed from a deliberately constructed boundary and reopened all six neurons asleep; a node fixture reruns finalization without raw reconversion. This allows migration to yield safely to interactive host workloads without losing completed work. | Implemented and fixture-proven; production use begins with the next executable restart | `crates/brain/src/streaming_migration.rs`, `crates/brain/src/brain.rs`, and `crates/node/src/brain_api.rs` |
| 2026-07-17 | Working set alone can hide catastrophic private commitment. At the fixed 9.7179 GiB output boundary, the fourth candidate reached 21.08 GiB private/pagefile memory while external trimming made its visible working set appear near zero. Each resume faulted roughly 5 GiB into RAM within seconds, dropping host availability as low as 2.59 GiB. Code-flow localization showed that the remaining legacy `sequences` and `cold_offsets` vectors were still deserialized wholesale after labels. Stream sequence recurrence bytes into a cold auxiliary record inside the same `.wbrain`, consume cold offsets entry-by-entry, and monitor private bytes as well as working set and available RAM. Adaptive pause/resume prevented a lockup, but paging is not a substitute for bounded data structures. | Causal production-scale failure localized; vector streaming fixture-tested | `crates/brain/src/streaming_migration.rs`, `crates/brain/src/store/container.rs`, `scripts/monitor_migration_memory.ps1`, and fourth production migration telemetry |
| 2026-07-17 | Preserving a legacy recurrence vector as one cold blob is insufficient if training ignores it or startup must hydrate it. Stream it as an on-disk hash ledger: bucket roots and collision links remain inside `.wbrain`, lookup reads only one bucket chain and the candidate's member IDs, and only sequences touched by the current experience enter the live delta map. A migrate/reopen fixture proved that a frame observed once before migration crosses its promotion threshold on the first post-migration recurrence without loading the complete ledger. | Architecture invariant implemented and fixture-proven; production-scale latency pending | `crates/brain/src/streaming_migration.rs`, `crates/brain/src/pool.rs`, and `crates/brain/src/store/container.rs` |
| 2026-07-17 | Sleeping neuron bodies are not enough if the runtime retains every concept's member vector in a resident deduplication map. Build the ordered concept index in a bounded temporary disk table while neurons stream, copy the finished hash table into `.wbrain`, and query only the relevant collision chain. A migrate/reopen fixture resolved the original cold concept ID on a repeated frame without creating a duplicate neuron; candidate-member comparison uses one scoped read rather than one seek per member. | Architecture invariant implemented and fixture-proven; production-scale latency pending | `crates/brain/src/streaming_migration.rs` and `crates/brain/src/pool.rs` |
| 2026-07-17 | A zero-resident-terminal claim can still hide full inference scans and retained concept payload. Sleeping concepts now release member vectors without losing concept identity; single- and multi-pool recall route through exact, feature-atom, and narrow motif postings, page only those binding bodies/member trees, and never use their former whole-binding-pool fallbacks. A fresh Windows CI runner passed all five streaming migration fixtures, the sleep/page-in identity round trip, lazy scoped single/multi recall, and node raw-complete/finalization recovery. | Executable-proven on Windows; production-scale latency pending | GitHub Actions run `29616946268`, `crates/brain/src/neuron.rs`, `crates/brain/src/brain.rs`, and `crates/brain/src/store/wbrain_store.rs` |
| 2026-07-22 | Exact ordered sensory evidence must preempt fuzzy feature and motif routing. Unioning broad fuzzy postings into an already exact route inflated the production working set by gigabytes and can allow weaker historical associations to displace the exact answer. | Causal production correction; exact, held-out, structural, and OOV gates passed | `docs/WBRAIN_SCOPED_PAGING_AUDIT.md` and `runtime/brains/programming-integrated-20260713/foundation.post-wbrain.json` |
| 2026-07-22 | Treat candidate breadth and neuron-body depth as separate configuration controls. Keeping up to 512 candidates from each posting protects recall, while selecting the eight most discriminative feature lists and eight motif lists bounds routing. Load only identity, labels, and membership for those candidates; hydrate terminals only for propagation or learning. Reducing candidate breadth to 128 harmed accuracy, whereas shape-only paging made 512 safe. | Ablation-supported and production-proven | `crates/brain/src/brain.rs`, `crates/brain/src/pool.rs`, and `docs/WBRAIN_SCOPED_PAGING_AUDIT.md` |
| 2026-07-22 | Do not update precision thresholds, salience, or other adaptive state merely because a decoder selected an answer. Read-only inference was reinforcing its own unverified choices and made before/after evaluations order-dependent. Apply adaptation only after an externally verified success or failure episode. | Causal correction; three clean-server code evaluations were identical at trained `5/5` and novel `5/5` | `crates/brain/src/brain.rs`, `crates/brain/src/pool.rs`, and `runtime/brains/programming-integrated-20260713/foundation.post-wbrain.json` |
| 2026-07-22 | Admit a production brain configuration only when behavioral accuracy and lifecycle resource gates pass together: exact, paraphrase, structural transfer, honest OOV, foundation retention, peak private memory, neurons awakened, and zero resident terminals after request cleanup. The current programming brain passed toddler `32/32`, K--12 `16/16`, OOV `3/3`, trained/novel code `5/5 + 5/5`, and debug `19/19` with a measured peak below 216 MiB. | Production-proven | `docs/WBRAIN_SCOPED_PAGING_AUDIT.md` and `runtime/brains/programming-integrated-20260713/foundation.post-wbrain.json` |
| 2026-07-22 | Apply evidence precedence consistently across pools: an unambiguous exact language+behavior episode outranks fuzzy raw-character motifs, while motifs remain the resolver when the sparse feature route is ambiguous. For project manifests, language compatibility must be derived from learned filenames/extensions rather than accepting every JSON `files` object. Missing-context inhibition must cover equivalent phrases such as “unspecified,” “not provided,” and “not specified.” Fixing these shared decision points raised the production enterprise battery from `9/12` to `12/12` with tick delta `0`, unchanged topology, and zero terminal residency afterward. | Causal production family sweep and executable admission | `runtime/benchmarks/enterprise-post-wbrain.json`, `runtime/benchmarks/enterprise-post-routing-fix-full.json`, `crates/node/src/brain_api.rs`, and `crates/brain/src/pool.rs` |
| 2026-07-22 | Rich prompts can retrieve several independently learned fragment families at once. Quarantine failures at the family/file boundary: an unbound parameter template, malformed candidate, conflicting slot, incomplete dependency, or cycle must not veto a separate dependency-complete family. Remain fail-closed when no complete family survives. Choose between surviving artifacts using requested shape: a single-class request prefers the dependency-composed class, while an explicit multi-file request prefers a complete file manifest. This restored the never-trained 12-discipline `AdaptiveCoordinator`: execution passed, all twelve premise ablations broke execution and removed their feature, the no-retry contradiction changed behavior, and tick delta remained `0`. | Causal production integration sweep | `runtime/benchmarks/multidomain-post-artifact-shape.json` and `crates/node/src/brain_api.rs` |
| 2026-07-22 | Corpus admission and rollback must guard the authoritative persisted representation. After migration, `brain.bin` is legacy input and `brain.wbrain` is the mutable brain. A hard link is not a valid `.wbrain` guard because slot records and appended bodies mutate the same inode. Comprehensive gates now publish an independent `.wbrain` copy atomically; fast canaries remain copy-free, and rollback restores the container plus rewinds WAL/progress together. | Operational invariant, contract-tested | `scripts/programming_curriculum_supervisor.py` and `tests/test_programming_runtime_contract.py` |
| 2026-07-22 | Separate container intent from behavioral breadth. `LANGUAGE:PYTHON + ARTIFACT:PROJECT + SECURITY:AUTHORIZATION` is one behavior, not a rich composition request, so bounded character-motif evidence must remain available to resolve competing authorization artifacts. Also quarantine a whole file when any retrieved requested fragment in that file has an unresolved dependency; otherwise imports and initialization can masquerade as a finished implementation. This raised semantic stress from trained `3/4` to `4/4` while held-out remained `4/4`; enterprise trained/paraphrase/OOV passed `5/5 + 5/5 + 3/3`, experiential transfer passed, and the 12-discipline synthesis plus all ablations still passed with tick delta `0`, zero resident terminals, and peak working set about 155 MiB. | Causal production routing sweep | `runtime/brains/programming-integrated-20260713/semantic-stress.json`, `runtime/benchmarks/enterprise-post-routing-fix.json`, `runtime/benchmarks/experiential-post-routing-fix.json`, `runtime/benchmarks/multidomain-post-routing-fix.json`, and `crates/node/src/brain_api.rs` |
| 2026-07-22 | Every mutation admission path must select and guard the authoritative persisted representation, not merely the corpus supervisor. The reproducible seed trainer and the shared experiential/multidomain transaction now copy mutable `.wbrain` containers independently and atomically, retain hard-link efficiency only for atomically replaced legacy `.bin`, and record storage/guard mode in metadata. Fresh reproduced nodes also explicitly enable lazy housekeeping and deferred promotion. In-place mutation-isolation and restore tests pass for both seed and experiential `.wbrain` guards; the complete runtime contract is `50/50`. | Cross-workflow invariant, contract-tested | `scripts/train_programming_brain.py`, `scripts/programming_experiential_generalization.py`, `docs/PROGRAMMING_BRAIN_REPRODUCTION.md`, and `tests/test_programming_runtime_contract.py` |
| 2026-07-22 | A maximum lock duration without corpus provenance is insufficient for scale diagnosis. MetaMath minimum-scope transactions reached `47.931` seconds near durable row `222976` and later `97.6343` seconds in the batch window ending at the `245760` canary, even though following transactions returned near `0.25–2.1` seconds and host memory recovered. Every over-ceiling transaction must therefore be appended to a durable `*.slow-batches.jsonl` ledger with exact logical row range, submitted episode count, encoded payload bytes, request result, ceiling, and lock scope before/after adaptation. Training continues; the recorded range becomes a focused micro-brain/performance experiment instead of forcing a full-stream halt. | Repeated live tail-latency observation; provenance pipeline contract-tested | `metamathqa-domain-safe.progress.json`, `tools/training_standard/drive_corpora_brain.py`, and `tests/test_programming_runtime_contract.py` |
| 2026-07-22 | A stopped corpus-worker boundary is the required rest state, not merely a convenient time to benchmark. Before admission, drain deferred promotions and maintenance, serialize all neuron bodies, checkpoint the settled state, and assert zero resident terminals; only then run behavioral retention. This makes the evaluated state identical to the next rollback base and prevents active-training residency from accumulating across multi-million-row phases. Continuous canaries intentionally skip settlement because their worker is still learning. | Lifecycle invariant implemented and contract-tested; next production block admission pending | `scripts/programming_curriculum_supervisor.py`, `docs/PROGRAMMING_BRAIN_REPRODUCTION.md`, and `tests/test_programming_runtime_contract.py` |
| 2026-07-22 | A rollback guard belongs to the curriculum phase and row that created it, not to the supervisor process globally. On restart, iterating already-complete earlier phases must never release a later phase's active guard. Every workflow acceptance now supplies the expected phase, and missing or mismatched ownership metadata preserves the guard. This was found when restart traversal deleted the MetaMath row-`180224` guard. The attempted row-`246784` replacement was later proven invalid because it copied an uncheckpointed base container; the active recommendation is superseded by the checkpoint-proof finding below. | Causal restart failure reproduced; ownership fixed, replacement claim retracted | `scripts/programming_curriculum_supervisor.py` and `tests/test_programming_runtime_contract.py` |
| 2026-07-22 | Once adaptive training reaches a one-episode lock scope, request-level timing ranges are unnecessarily broad. Return the zero-based slowest-lock chunk index and length from the node, retain the logical source row for every submitted episode (including repetitions and skipped-row gaps), and write their exact mapping into the slow-batch ledger. Older nodes must report the exact mapping as unknown rather than guessing row zero. This turns the next scale-only tail into a directly reproducible micro-brain case while the accepted stream continues. | Implemented and contract-tested; production activation waits for the next safe node boundary | `crates/node/src/brain_api.rs`, `tools/training_standard/drive_corpora_brain.py`, and `tests/test_programming_runtime_contract.py` |
| 2026-07-22 | Replay slow ranges unchanged on disposable micro-brains before altering corpus configuration. Five MetaMath ranges whose production one-episode lock maxima were `8.6492–83.9147` seconds each completed all 256 episodes on fresh brains in `0.2041–0.2836` seconds total; their slowest individual episodes were only `0.0030–0.0067` seconds. Payload length therefore does not explain the tails: accumulated-state cold lookup, recurrence, binding, or housekeeping does. The node now attributes the slowest lock across those stages so the next production event can identify the responsible subsystem without stopping the stream. | Causal parallel micro-brain scale isolation; stage telemetry implemented | `scripts/programming_slow_batch_microbrains.py`, `runtime/benchmarks/slow-batch-microbrains.json`, `crates/brain/src/brain.rs`, and `crates/node/src/brain_api.rs` |
| 2026-07-22 | Do not blame lazy tick housekeeping for a long training lock without measuring it. During repeated `10–53` second minimum-scope tails, the live cumulative tick profile averaged about `400` microseconds total and only `3` microseconds of housekeeping per tick, while the node sustained roughly `4.3–4.8 MiB/s` of reads. The deployable profiler therefore splits frame work into atom encoding/lookup, atom touch, cold action-concept lookup, sequence recurrence, and concept write in addition to binding and tick stages. | Housekeeping ruled out at production scale; cold-read hypothesis narrowed, final stage attribution pending safe activation | `/brain/tick_profile`, `crates/brain/src/pool.rs`, `crates/brain/src/brain.rs`, and `crates/node/src/brain_api.rs` |
| 2026-07-22 | WAL acknowledgement and a snapshot rollback guard are different durability boundaries. A guard may be labeled with corpus row `R` only after the live node completes `/brain/checkpoint`; record the post-barrier topology in guard metadata, then require the reopened topology to match it before rewinding the ledger to `R`. Copying only `brain.wbrain` while accepted changes remained in WAL/RAM produced a nominal row-`246784` guard whose reopened topology exactly matched the earlier admitted row `180224`. Automatic recovery compounded the error by trusting a stale PID file and accepting health from the old listener. Recovery now resolves the endpoint listener, refuses unrelated or pre-existing owners, confirms the launched PID owns the port, and verifies topology after restore. | Production failure causally reconstructed; protocol corrected and `55/55` contracts pass | `scripts/programming_curriculum_supervisor.py`, `tests/test_programming_runtime_contract.py`, `metamathqa-domain-safe.row-180224.retention-gate.json`, and `curriculum-health.jsonl` |
| 2026-07-22 | Do not launch a fast canary merely because the progress ledger reached the comprehensive block endpoint. The worker publishes its WAL-durable row before its final container checkpoint returns, so a canary started at that instant can queue behind the serialization lock and time out even though training succeeded. Fast canaries are now restricted to interior block boundaries; the stopped-worker comprehensive gate owns the endpoint. Transport timeouts are classified as recoverable gate failures rather than uncaught supervisor crashes. | Boundary race reproduced and corrected; `56/56` contracts pass | `scripts/programming_curriculum_supervisor.py` and `tests/test_programming_runtime_contract.py` |
| 2026-07-22 | After reconciling the false row-`246784` guard to the topology-proven row `180224`, replaying the next 16,384 MetaMath rows produced no new transaction above the eight-second ceiling. The settled row-`196608` candidate then passed recall `32/32`, all foundation/debug transfer checks, honest OOV, and enterprise `12/12` with tick `460945 -> 460945`, unchanged topology, and zero resident terminals. This proves the recovered boundary and replay were behaviorally valid; it does not yet eliminate scale-dependent tails at later accumulated-state sizes. | Production-proven and admitted | `runtime/brains/programming-integrated-20260713/metamathqa-domain-safe.row-196608.retention-gate.json` |
| 2026-07-22 | Deep stage attribution localized accumulated-state training tails to cold fingerprint scalar lookup. Exact rows `196763` and `202230` spent `23.5422s` and `15.3079s` in promoted/tentative binding lookup while all frame/neurogenesis/tick work remained milliseconds; row `198057` spent `17.0654s` in the preceding lifetime-recurrence lookup. A first lifetime observation mathematically proves that no promoted or tentative binding predates it, so skip those two redundant cold scans. At restore, classify the tiny auxiliary-record references once and make scalar state search only generations carrying the fingerprint marker rather than every binding-route generation. This retains neuron-scoped payload paging and changes neither recurrence nor binding identity. All 82 brain-library tests pass serially, including migration, legacy pair-only recurrence, reopen, and lazy recall; generation counts are now exposed in node stats for the production ablation. | Causal production localization; correction fixture-proven, production deployment pending safe boundary | `metamathqa-domain-safe.progress.slow-batches.jsonl`, `crates/brain/src/brain.rs`, `crates/brain/src/store/posting_index.rs`, and `crates/brain/src/store/wbrain_store.rs` |
| 2026-07-22 | A continuous canary must own an explicit read-only residency scope. Releasing every resident after inference discarded 107 newly trained binding neurons that predated the canary and had not reached the `.wbrain` slot directory; later neurogenesis then exposed the gap at idle serialization (`473956` versus logical extent `473849`). Start one multi-pool inference scope, record only neurons demand-paged inside it, and release exactly that set. Pre-existing residents can contain uncheckpointed learning and remain live until neuron-wise serialization. The regression preserves three unsaved neurons across query cleanup, serializes them contiguously, and reloads every stable ID. In production, WAL recovery retained the row-`221184` candidate, serialized all `25101` residents without a gap, and admitted it with corpus recall `32/32` (maximum `0.2342s`), enterprise `12/12`, full foundation/debug/OOV retention, tick delta `0`, unchanged topology, and all `532616` neurons asleep. | Causal correction, fixture- and production-proven; complete serial brain library `83/83` | `crates/brain/src/pool.rs`, `crates/brain/src/brain.rs`, `crates/brain/src/store/wbrain_store.rs`, and `runtime/brains/programming-integrated-20260713/metamathqa-domain-safe.row-221184.retention-gate.json` |
| 2026-07-22 | The cold-fingerprint generation filter changed production training from a `6.7801s` pre-deployment batch EMA to `0.0415–0.1166s` across the next admitted block, with typical final intervals near `0.04–0.06s`. From MetaMath row `221184` to `352256`, the brain accepted `114688` episodes, skipped the already quarantined `16384`-row interval, passed seven continuous canaries, and added no event to the 42-entry historical over-eight-second ledger. The stopped-worker gate then passed recall `32/32`, enterprise `12/12`, full foundation/debug/OOV retention, tick delta `0`, unchanged topology, and zero resident terminals across `648025` neurons. Active training residency grew within the guarded block, then neuron-wise sleep raised host-available RAM from roughly `5–6 GiB` to `12 GiB`; configuration must therefore distinguish bounded request-time inference from an intentionally live training window and schedule durable sleep at guarded boundaries. | Production ablation and full-block admission | `metamathqa-domain-safe.progress.json`, `metamathqa-domain-safe.progress.slow-batches.jsonl`, `curriculum-health.jsonl`, and `metamathqa-domain-safe.row-352256.retention-gate.json` under the active runtime |
| 2026-07-22 | The optimized brain completed MetaMath at row `385524` with its original suspect interval still isolated, rather than allowing a known failure range to contaminate later training. The completion gate sampled the entire accepted history at `64/64` recall (mean `0.1066s`, maximum `0.3866s`), retained toddler `32/32`, K--12 `16/16`, Python trained/novel `5/5 + 5/5`, TypeScript trained/paraphrase/OOV `3/3 + 3/3 + 3/3`, and enterprise `12/12`; tick and structure were unchanged and all `681570` neurons were asleep. A quarantine ledger is therefore compatible with a strong whole-corpus completion claim only when the claim explicitly excludes the deferred interval and the full accepted-history sample remains clean. | Full-corpus production completion, one interval explicitly deferred | `runtime/brains/programming-integrated-20260713/metamathqa-domain-safe.completion-gate.json` and `runtime/brains/programming-integrated-20260713/curriculum-deferred-intervals.jsonl` |
| 2026-07-25 | Corpus rows are logical coordinates, not unique brain states. After rollback, CodeSearchNet revisited row `16896` with a new combination of skipped and newly trained data, but the supervisor reused a green canary from the earlier state and emitted the impossible interval `16896:16896`. Scope passing canaries to the current last-good guard epoch, require the green boundary to precede the failed candidate, and reject malformed or zero-width half-open ranges at the ledger fold. Subtract the union of existing quarantines from the failed window and record every remaining disjoint span; never wrap an outer interval around already deferred data. Because training continues while a fast read-only canary runs, stop the worker first and extend causal coverage through its final durable row. Code canaries now retain the exact prompt, selected code, expected training response, execution inputs/result, and console error so a behavioral regression is directly reproducible instead of represented only as `4/5`. | Production recovery failure causally reconstructed; interval and diagnostic contracts pass `64/64` | `scripts/programming_curriculum_supervisor.py`, `scripts/programming_code_eval.py`, `tests/test_programming_runtime_contract.py`, and the active runtime's `csn-python-full.stderr.log` |
| 2026-07-25 | A response can be syntactically valid in the requested language while being behaviorally unrelated. CodeSearchNet row `34118` taught Redis `incrby`; its sparse intent and the protected word-frequency paraphrase both collapsed to `LANGUAGE:PYTHON + STATE:INCREMENT_COUNT`. Their raw character-motif margin was only `0.00274`, so ranked sparse readout returned `incrby` for `word_freq`. Extract `TEXT:WORD_FREQUENCY` independently from word/frequency/occurrence plus collection-shape cues, and require a candidate for that intent to contain grounded word/token/split/counting evidence before accepting it as language-compatible. This preserves the broader increment feature while preventing a valid-Python false positive from satisfying the wrong behavioral contract. Reopening the exact rejected row-`34304` candidate under the corrected release changed the protected Python result from trained/novel `5/5 + 4/5` to `5/5 + 5/5` without retraining, proving the correction acts on the real accumulated collision rather than only a fixture. The later comprehensive settled gate admitted the correction and resolved its exact candidate intervals. | Exact production collision captured; causal routing correction fixture-, rejected-state-, and comprehensive-admission-proven | `runtime/brains/programming-integrated-20260713/csn-python-full.row-34304.retention-gate.json`, corpus row `34118`, `crates/brain/src/pool.rs`, `crates/node/src/brain_api.rs`, and `scripts/programming_curriculum_supervisor.py --retest-canary-quarantine` |
| 2026-07-25 | Intent extraction must use identifier boundaries, and derived exact bindings still require behavioral admission. CodeSearchNet logical row `42072` described Python **keyword** arguments, a dictionary, and the number of times a mock was called. Substring matching treated the `word` inside `keyword` as a word-frequency cue; count/dictionary cues then produced the same sparse labels as the protected `word_freq` task. The new derived binding selected `__get_call_count` exactly and bypassed the compatibility checks applied to ranked candidates. A later old-release canary reproduced the family twice: an average request selected unrelated `grid_time`, and word frequency selected `make_password`. Match semantic terms at ASCII identifier boundaries (`word`/`words`, never `keyword` or `password`), require average output to perform a mean computation, require word-frequency output to combine segmentation with aggregation (or a grounded `Counter`), and apply behavioral compatibility to exact/composed artifacts reconstructed from derived pools. Preserve a complete raw sensory episode as authoritative: compatibility guards derived generalization, not exact atom-grounded experience. After rollback and deployment, the corrected release's next production canary retained Python trained/novel `5/5 + 5/5`, accepted-history recall `8/8`, toddler `32/32`, and K--12 `16/16`; exact quarantined-row replay remains deliberately pending the comprehensive admission gate. | Production collision family causally diagnosed; intent `32/32`, node routing `25/25`, runtime contracts `69/69`, and corrected-release canary green; quarantine replay pending | corpus logical row `42072`, intervals `csn-python-full:34304:50176` and `50432:66304`, `crates/brain/src/pool.rs`, `crates/node/src/brain_api.rs`, and the active runtime's `curriculum-health.jsonl` |
| 2026-07-25 | Direct deterministic sensory features outrank fuzzy learned semantic refinement. After the row-`34304` candidate was settled and its WAL incorporated, the average paraphrase directly emitted `LANGUAGE:PYTHON + MATH:AVERAGE + GUARD:EMPTY_INPUT`, but a character-motif route with score `0.394958` and margin `0.043894` deleted `MATH:AVERAGE`. Language plus the generic guard then admitted unrelated Redis `srem`. A learned route may recover intent when direct evidence cannot ground a feature pool, but it must not erase a directly observed behavior; over-broad deterministic extraction must be corrected at its source. Behavioral compatibility also needs semantic shape checks, because language-valid code is not task-valid code. Average, odd-parity, negative-comparison, and word-frequency intents now reject unrelated sources lacking their corresponding computation evidence. The same settled production candidate returned to Python trained/novel `5/5 + 5/5`, preserved tick `634564`, and released all paged neurons after the correction. | Causal settled-production family sweep; node routing `25/25` and intent encoding `32/32` | `runtime/brains/programming-integrated-20260713/csn-python-full.row-34304.foundation.json`, `crates/node/src/brain_api.rs`, and the row-`34304` post-correction code evaluation |
| 2026-07-25 | Context is a ranking signal over grounded sparse-feature candidates, not authority to erase them. At the settled row-`34304` enterprise gate, 11 of 12 suites passed, but CodeSearchNet context displaced the atomic-transfer manifest for one held-out account-movement prompt and selected unrelated Python audit code. Union the bounded independently grounded feature set with the context-weighted set, retaining weighted order while making independent evidence available as a fallback. Apply behavioral compatibility to both plain source and every source file inside a manifest; language-shaped output alone cannot satisfy authorization, idempotency, atomic transfer, correlated logging, or secret-redaction intent. Sweeping all four semantic-stress families raised trained/held-out recall from `4/4 + 3/4` to `4/4 + 4/4` on the same settled production state while Python remained `5/5 + 5/5`, tick stayed `634564`, and all `682617` neurons returned to sleep. | Causal settled-production context ablation and behavior-family sweep | `runtime/brains/programming-integrated-20260713/semantic-stress.post-family-compatibility.json`, `runtime/brains/programming-integrated-20260713/csn-python-full.row-34304.enterprise.json`, and `crates/node/src/brain_api.rs` |
| 2026-07-25 | A quarantine correction is not admitted by a focused green rerun. Retest the exact candidate only after stopped-worker settlement and checkpoint, then require distributed accepted-history recall, every protected foundation and executable-transfer check, the complete enterprise battery, read-only tick/topology stability, and zero idle residency in one authoritative transaction. The corrected CodeSearchNet row-`34304` candidate recalled `32/32` distributed samples (mean `0.0606s`, maximum `0.3729s`), retained toddler `32/32`, K--12 `16/16`, Python trained/novel `5/5 + 5/5`, debug exact/held-out/structural `6/6 + 6/6 + 4/4`, semantic stress trained/held-out `4/4 + 4/4`, honest OOV, and enterprise `12/12`. Tick remained `634564`, topology stayed at `13` pools and `682617` neurons, and every neuron returned to serialized idle state. Only then were exact intervals `16640:16896` and `33536:34304` resolved and the rollback guard released. | Comprehensive settled-production admission | `runtime/brains/programming-integrated-20260713/csn-python-full.row-34304.retention-gate.json`, `csn-python-full.row-34304.enterprise.json`, and `curriculum-deferred-intervals.jsonl` under the active runtime |
| 2026-07-25 | Quarantine storage has the same admit/resolve lifecycle as the logical interval. An immutable causal base is required while any interval references it, but retaining resolved 37.8 GiB bases silently exhausts the volume and can make the next rollback impossible. Fold the append-only ledger before cleanup, delete only digest directories belonging to known resolved intervals, protect a replacement sub-interval's recorded outer base as well as its own digest, preserve active and unknown experiment directories, and account for hard links correctly. Before publishing a new independent `.wbrain` guard, require free space for the guard, one later rollback replacement, and reserve; fail before copying when that recovery invariant cannot be met. Production cleanup removed three resolved directory names representing two inodes, reclaimed about `75.7 GiB` (`108.0 -> 183.7 GiB` free), and retained all four unresolved causal bases. | Lifecycle correction contract-tested `66/66` and production-proven | `scripts/programming_curriculum_supervisor.py`, `tests/test_programming_runtime_contract.py`, and the active runtime's `deferred/` directory |
| 2026-07-25 | A historical maximum is a safe starting calibration, not a permanent upper bound after the causal bottleneck has been removed. Start a resumed accumulated-state worker at the cross-phase conservative lock scope; after eight consecutive production observations below one quarter of the live-lock ceiling, double only the server-side neural lock chunk (`1 -> 2 -> 4 -> 8 -> 16 -> 32`) while leaving the 256-episode network batch and episode order unchanged. Reset the evidence streak on middling latency, and immediately scale downward plus retain exact row/stage provenance on any ceiling breach. This is live-state AIMD over orchestration only: it changes neither accepted examples nor their deterministic ordering. The first corrected production worker started at scope `1`, passed its canary, then traversed the entire `1 -> 2 -> 4 -> 8 -> 16 -> 32` schedule with zero reductions. Its first scope-`32` observation held the neural lock for `0.6767s` against the `8s` ceiling. | Runtime-contract-proven `69/69` and full production calibration proven through configured scope `32` | `tools/training_standard/drive_corpora_brain.py`, `scripts/programming_curriculum_supervisor.py`, `tests/test_programming_runtime_contract.py`, and `csn-python-full.progress.json` under the active runtime |
| 2026-07-25 | Guard creation is part of the mutation transaction and requires single-writer ownership even before the corpus worker exists. Concurrent supervisor launches can race on `brain.last-good.wbrain.tmp`; one owner may be streaming the 37.9 GiB copy while another treats the same temporary path as stale. Detect an existing Python supervisor for the same resolved runtime, then claim a PID file with exclusive creation and recover only demonstrably stale claims. The surviving production owner completed its guard and advanced CodeSearchNet durably; future launches reject the duplicate before touching snapshot or progress state. | Production race observed; single-owner contract-tested `68/68` | `scripts/programming_curriculum_supervisor.py`, `tests/test_programming_runtime_contract.py`, and `curriculum-supervisor-forward.stderr.log` under the active runtime |
| 2026-07-25 | Several quarantined intervals can begin from the same accepted brain even when rollback regenerates the guard filename and inode between attempts. Derive a causal-state identity only from checkpoint-proven phase, row, storage type, tick, generation counters, and full topology; intervals with that exact identity hard-link one immutable base, while missing proof or any state difference forces a distinct base. This avoids paying roughly one complete `.wbrain` per failure from a shared boundary without treating logical row equality alone as state equality. | Implemented and runtime-contract-proven `70/70`; activation begins with the next supervisor process | `scripts/programming_curriculum_supervisor.py` and `tests/test_programming_runtime_contract.py` |
| 2026-07-25 | Quarantine-forward training is incomplete unless deferred knowledge can be transactionally merged into the final state. After every forward phase completes, replay one folded unresolved interval at a time against the final brain: independently guard that final state, train only the exact half-open range, recall within that range, then run full-corpus, foundation, execution, enterprise, tick, topology, and idle-residency admission with only that interval removed from the skip set. Resolve and prune only on a complete pass. On any failure, restore the final guard, retain the interval and diagnostics, and stop for a family-wide correction before retrying. | End-of-corpus orchestration implemented and runtime-contract-proven `72/72`; production use awaits forward completion | `scripts/programming_curriculum_supervisor.py --replay-deferred`, `tests/test_programming_runtime_contract.py`, and `docs/PROGRAMMING_BRAIN_REPRODUCTION.md` |
| 2026-07-25 | Semantic evidence needs identifier boundaries at both extraction and response admission. CodeSearchNet row `94564` trained `MXProfileAdjustCounter`; the protected word-frequency paraphrase later accepted its `increment` method because a raw `contains("counter(")` check found the suffix inside `AdjustCounter(`. Recognize `Counter(...)` only when the callable name begins at an identifier boundary, just as `word` must not match inside `keyword`. Apply the same behavioral contract to the autonomous multi-pool answer after propagation: confidence and multiple co-firing pools establish support, not task compatibility. This closes exact, ranked, motif, composed, raw-fuzzy, and autonomous derived exits while leaving complete exact sensory episodes authoritative. | Production collision causally captured; corrected production-bin routing `25/25`, runtime contracts `74/74`, and first corrected-release live canary at row `50432` passed recall `8/8`, trained and paraphrased execution `5/5` each, toddler `32/32`, K-12 `16/16`, and OOV `3/3`; quarantined interval replay pending | corpus logical row `94564`, interval `csn-python-full:81920:100608`, `crates/node/src/brain_api.rs`, and the row-`50432` continuous-canary record in `curriculum-health.jsonl` |
| 2026-07-25 | A read-only evaluator transport failure is not evidence that the neurons learned an incorrect relationship. At the row-`163840` canary, seven preceding corrected-release canaries were green, the worker reached its exact durable block boundary, and `programming_brain_eval.py` then raised `TimeoutError` while the shared host was CPU-saturated. Classify timeout, reset, refusal, disconnect, and URL transport signatures separately from recall or execution mismatches; retry each affected canary stage with durable audit events. If infrastructure remains unavailable, stop the worker and retain the guarded candidate without publishing a semantic quarantine. Only a completed evaluator that reports a behavioral mismatch may create a deferred corpus interval. In the corrected production run, all four attempts again timed out on the saturated host and were recorded with `passed: null`; exhaustion stopped exactly at the durable block boundary without rollback and without changing the seven-interval semantic ledger. After host pressure fell, the same protected candidate passed the complete foundation gate at row `165377`, proving that the pause preserved useful learning rather than misclassifying infrastructure as knowledge corruption. | Production failure, natural retry exhaustion, safe pause, and subsequent candidate recovery proven; runtime contracts `86/86` | `scripts/programming_curriculum_supervisor.py`, `tests/test_programming_runtime_contract.py`, `csn-python-full.row-165377.foundation.json`, and the retry/pause events in `curriculum-health.jsonl` |
| 2026-07-26 | A successful `.wbrain` migration does not by itself reclaim duplicate legacy storage. After proving that the active identity and loader select `brain.wbrain`, stop the node, isolate the old `brain.bin`, milestone `brain.tick*.bin` candidates, abandoned posting-index temporaries, and `cold/*.cold` files, then reopen from `.wbrain` with the legacy cold directory absent. Reclaim legacy allocation only after accepted-corpus recall, foundation, OOV honesty, trained execution, and unseen paraphrase execution all pass. Finally prune resolved deferred causal bases through the append-only ledger rather than deleting interval directories by age. This recovered guard/rollback headroom without weakening neuron-addressable storage. | Production reopen without legacy cold passed recall `8/8`, toddler `32/32`, K-12 `16/16`, OOV `3/3`, trained execution `5/5`, and unseen paraphrases `5/5`; free space increased from `50.14` to `118.58` GiB | active runtime `brain.wbrain`, `load_or_build_brain`, and `prune_resolved_deferred_bases` |
| 2026-07-26 | Infrastructure classification is an admission-wide invariant, not a continuous-canary special case. The same retry and durable `passed: null` pause policy must surround idle stats, sleep, checkpoint, corpus recall, foundation, code, TypeScript, enterprise, quarantine-retest, and deferred-replay evaluation. Empty or truncated evaluator JSON is also an incomplete observation, not a learned regression. Exhausted retries preserve the guarded candidate (or transactionally restore a deferred replay) without adding a semantic interval; only a completed behavioral assertion may invoke rollback and quarantine. | Family-wide implementation and runtime-contract proof `86/86`; production activated at the row-`165377` settlement/foundation/enterprise admission | `scripts/programming_curriculum_supervisor.py`, `tests/test_programming_runtime_contract.py`, and the row-`165377` admission artifacts under the active runtime |
| 2026-07-26 | A narrow canary can advance only the causal boundary for the contract it actually evaluated. The fast CodeSearchNet canary checked accepted-row recall, foundation, and small executable-code families; it did not run the twelve-suite enterprise battery. When the stopped-worker enterprise gate at row `165377` found eight failing suites, using the last green fast-canary row `147456` would have attributed only `147456:165377` even though enterprise behavior had not been comprehensively admitted since the row-`34304` guard. Comprehensive midphase, completion, and attached gates must begin their suspect window at the last comprehensive guard and subtract the union of existing deferred ranges. Otherwise unproven rows between a narrow canary and the guard can be silently retrained as though they had passed the broader contract. | Production attribution flaw captured; comprehensive/narrow scope separation implemented and runtime-contract proven `86/86`; active ledger correction follows rollback completion | `scripts/programming_curriculum_supervisor.py`, `tests/test_programming_runtime_contract.py`, `csn-python-full.row-165377.enterprise.json`, and `curriculum-health.jsonl` |
| 2026-07-26 | Quarantine replay can expose a decoder defect even when the restored neural guard is unchanged. Replaying the fully deferred `34304:165377` CodeSearchNet block skipped all `131072` rows and left tick `634564`, yet the clean control passed only `8/12` enterprise suites under accumulated-state routing. Preserve each ranked action's neural evidence—matched feature atoms, learned feature width, outcome valence, use count, and binding identity—through decoding rather than reconstructing intent from emitted bytes. For project composition, search a dependency-compatible subset of grounded manifests, require every requested language and behavior family, and let unrelated same-filename candidates conflict only with their own subset. Add independent guard atoms such as positive batch size instead of folding them into generic batching. This distinguishes corpus damage from a scale-only readout collision and permits one family-wide decoder repair before any deferred rows are retrained. | Clean-guard control localized four remaining decoder families; evidence-preserving and compatible-subset correction passed feature-binding `8/8`, node routing `27/27`, serial brain `84/84`, and runtime-contract `86/86`, then improved the production guard from `8/12` to `11/12` with tick and structure unchanged | `crates/brain/src/brain.rs`, `crates/brain/src/pool.rs`, `crates/node/src/brain_api.rs`, `csn-python-full.row-165376.enterprise.json`, and `csn-python-full.row-165376.post-routing.enterprise.json` |
| 2026-07-26 | Semantic requirements are not a one-to-one estimate of how many grounded manifests must be composed. One component can implement several co-firing behaviors—for example, the same JavaScript order service provides command idempotency and a transactional outbox. Search the smallest bounded manifest subset (two through the configured upper bound) and admit it only when the aggregate source satisfies every requested language and behavior. Requiring `max(language_count, behavior_count)` manifests rejected correct polyglot paraphrases even though both needed components had been recalled. | First accumulated-state release improved the clean production guard from `8/12` to `11/12`; both remaining polyglot failures recalled exactly two correct candidates but demanded three manifests. Smallest-valid-subset correction passed node routing `28/28`, focused polyglot projects `6/6`, component executions `12/12`, OOV honesty `2/2`, and the full production enterprise guard `12/12` with tick `634564`, structure, and zero residency unchanged | `requested_manifest_component_count`, `merge_grounded_file_manifests`, `programming_polyglot_composition.py`, `polyglot.json`, `polyglot-smallest-valid-subset.json`, and `csn-python-full.row-165376.post-polyglot.enterprise.json` |
| 2026-07-26 | A durable worker position exactly equal to `last_good_row + gate_rows` means a comprehensive admission is due; it is not a zero-progress worker failure. Supervisor restarts must test that boundary before launching another worker. Route both a freshly reached boundary and a rediscovered stopped boundary through one admission transaction so infrastructure pause, semantic quarantine, automatic rollback, guard release, and status publication cannot diverge. | A production relaunch at row `165376` with guard row `34304` and gate width `131072` reproduced the zero-work stall without changing tick or topology; exact-boundary admission and the shared transaction passed runtime contracts `87/87` | `guarded_admission_due`, `admit_midphase_candidate`, the main supervisor loop, and `test_exact_guarded_boundary_is_admitted_after_supervisor_restart` |
| 2026-07-26 | Active training is allowed to retain its current learning scope until a guarded boundary, but shared-host memory pressure must not be allowed to lock the computer. Treat three consecutive low-memory observations as a lifecycle yield: stop only after RAM and WAL-durable corpus offsets agree, neuron-wise sleep and checkpoint the still-unadmitted candidate, keep its existing rollback guard, wait for memory to recover, and resume from the same row. Never quarantine rows or mark an incomplete block admitted because the host needed memory. | The configurable `6 GiB` production floor triggered at CodeSearchNet row `317184`: RAM and durable offsets agreed, 316 neuron bodies were serialized, resident terminals fell from `27,634,052` to zero, host-available memory rose from `5.37` to `7.47 GiB`, tick stayed `638308`, and training resumed at the same row. A second automatic yield at row `320000` again reached zero residency and raised availability from `5.97` to `7.84 GiB`. No row was admitted, duplicated, rolled back, or quarantined by either resource event. | `memory_floor_breached`, `resource_bounded_settlement`, `resource_waiting`, `scripts/programming_curriculum_supervisor.py`, `tests/test_programming_runtime_contract.py`, and the active runtime's `curriculum-health.jsonl` |
| 2026-07-26 | Neuron-wise serialization can release an active training working set without changing corpus coordinates. At CodeSearchNet row `202241`, an atomic sleep pass held the worker at the same durable row, serialized 491 resident neuron bodies, reduced node working set from `3.38 GiB` to `0.20 GiB`, and raised host-available RAM from `6.83 GiB` to `9.21 GiB`. The unchanged worker then advanced to row `203009` without rollback, quarantine, restart, or duplicated progress. The automatic circuit breaker adds a stopped-worker checkpoint before applying this proven lifecycle operation. | Production-proven manually and through repeated automatic stopped-worker settlement at rows `317184` and `320000`; the supervisor resumed each time from the exact durable coordinate | `runtime/brains/programming-integrated-20260713/csn-python-full.row-202241.resource-settlement.json`, `/brain/sleep`, `scripts/programming_curriculum_supervisor.py`, and the active runtime's `curriculum-health.jsonl` |
| 2026-07-28 | Long atom-grounded sequences are valuable configuration evidence, but persisting the complete sequence once for every binding posting made sleep cost scale with repeated sensory-key length. Separate routes by access contract: exact binding routes may persist a full 256-bit BLAKE3 identity in `W1ZPOST2`, while recurrence/fingerprint generations must retain their original bytes because maintenance scans them by prefix. Keep legacy combined generations readable by both indexes and mark new fingerprint-only generations explicitly. This preserves character grounding and collision resistance without turning long source episodes into multi-gigabyte repeated-key deltas. | Controlled long-key fixture used two 4,096-byte postings plus one 3,072-byte posting and made the hashed body more than `20x` smaller; hashed point lookup, torn-tail resume, legacy restore, prefix rejection, neuron-wise sleep, full brain library `86/86`, all brain integration binaries, node tests, and runtime contracts `88/88` passed. The first production settlement at row `317184` flushed a `1,165,417`-entry binding overlay and a `7,488`-entry fingerprint overlay into one new generation of each type, zeroed both overlays, reopened the same state, and resumed training. | `crates/brain/src/store/posting_index.rs`, `crates/brain/src/brain.rs`, `crates/brain/src/store/wbrain_store.rs`, `crates/brain/src/pool.rs`, and the row-`317184` `resource_bounded_settlement` record in the active runtime |
| 2026-07-28 | Immutable deferred causal bases may use transparent filesystem compression when guard copies would otherwise exhaust the volume. Compression is an operational storage setting, not permission to delete unresolved experiments: prove the snapshot header and ordinary reads after compression, retain its logical path and inode, and run compaction below normal priority while the shared host remains usable. | The first `28,903,562,262`-byte deferred `.wbrain` remained transparently readable with its `W1ZBRAIN` header and occupied `5,196,255,232` bytes after LZX compression, a `5.6:1` ratio. The second `40,632,172,842`-byte causal base occupied `6,482,829,312` bytes (`6.3:1`), transparently returned the `W1ZBRAIN` v4 header, and restored independent-guard headroom to about `57.3 GiB` free while training remained usable. | `runtime/brains/programming-integrated-20260713/deferred/66a8432cff5d23da/brain.base.wbrain`, `deferred/7a9df07670a7d55a/brain.base.wbrain`, and their compact logs |
| 2026-07-28 | A per-word frequency map is not the same behavior as a scalar word count or a file-statistics report. A quarantined CodeSearchNet candidate shared splitting and generic `+ 1` syntax with `word_freq`, so broad source-shape compatibility admitted a `wc(filename, contents, ...)` routine that counted words, paragraphs, and characters but never produced `word -> occurrences`. Configure the intent pool to emit `TEXT:WORD_FREQUENCY` only for explicit per-word distribution evidence (`dict`, mapping, frequency, repeated/every/distinct word, or `word_freq`). At readout require word segmentation plus a dictionary accumulator (`get`, `defaultdict`, `setdefault`, indexed increment) or an imported standard `Counter`; a generic function named `counter`, `len(split(...))`, or unrelated paragraph increment is insufficient. | Causal family fixture includes the exact observed `wc` shape, scalar word count, misleading `counter` name, generic increments, and three valid accumulator families. Brain library `86/86`, instruction-intent `32/32`, and brain-server routing `29/29` pass. After release deployment, the exact live paraphrase returned the grounded `word_freq` implementation and the protected execution suite passed trained and novel prompts `5/5 + 5/5` with unchanged tick/topology and zero idle residency. | `crates/brain/src/pool.rs`, `crates/brain/tests/instruction_intent_encoding.rs`, `crates/node/src/brain_api.rs`, `scripts/programming_code_eval.py`, and the `csn-python-full:296448:313600` quarantine evidence in `curriculum-health.jsonl` |
| 2026-07-28 | Guard capacity is one accepted `.wbrain` plus reserve, not two simultaneous full containers. The rollback authority is the immutable accepted guard and the causal failure artifact is the quarantined corpus interval. Once admission rejects a candidate and the node is stopped, that derived live inode may be deleted before restoring from the guard if the volume cannot hold both candidate and replacement; an interrupted copy remains retryable from the untouched guard. Requiring space for guard + candidate + replacement unnecessarily halted otherwise safe training at scale. | Transactional low-space fixture proves the rejected inode is the only state discarded, the accepted guard survives byte-for-byte, and restoration remains pending until explicit finalization; runtime contracts `90/90` pass | `ensure_last_good_guard`, `restore_canary_quarantine`, `tests/test_programming_runtime_contract.py`, and `docs/PROGRAMMING_BRAIN_REPRODUCTION.md` |
| 2026-07-28 | A behavior name is not evidence that a function implements that behavior. CodeSearchNet trained a temporal-duration method declared as `average(self, dt=None)` that returned the midpoint between two times; a protected arithmetic-mean request later selected it because response admission treated the bare substring `average(` as sufficient. Require an actual collection aggregation (`sum` with a cardinality denominator) or a qualified statistics/array operation such as `.mean`, `fmean`, or `.average`; a declaration name or division by two alone is insufficient. Apply the same rule to ranked, reconstructed, composed, and autonomous derived exits while preserving an exact atom-grounded episode. | Exact row-`327680` canary failure isolated the candidate interval `313856:331264`; the temporal-midpoint fixture is rejected on both normal and derived-artifact routes, valid scalar and NumPy mean implementations remain accepted, and all node package groups pass (`1`, `28`, `29`, and `59` tests). After corrected-release deployment and exact guard restore, the authoritative row-`313856` production canary passed accepted-history recall `8/8`, toddler `32/32`, K–12 `16/16`, OOV `3/3`, and trained/unseen Python execution `5/5 + 5/5`; the concurrent worker skipped the quarantined family and advanced durably to row `333568`. | `crates/node/src/brain_api.rs`, `scripts/programming_code_eval.py`, and the row-`327680` failure plus row-`313856` corrected-release canary records in the active runtime's `curriculum-health.jsonl` |
| 2026-07-28 | Recurrence state has two different access contracts and should not pay the more expensive one universally. Lifetime count and tentative/promoted binding IDs are exact point lookups, so persist them by full BLAKE3 identity in a hashed scalar generation. Only a recurrent fingerprint that still has no binding needs its full atom-grounded sequence in a separately marked, prefix-enumerable maintenance generation for `force_promote_tentative`. Legacy combined and first-generation split indexes remain both point-readable and enumerable. This preserves atom grounding and force-promotion semantics while preventing a direct-pretrained code episode from repeating its long fingerprint in lifetime, tentative, and promoted scalar records. | A 128 KiB promoted atom-grounded fixture produced one hashed scalar generation and zero full-key candidate generations after sleep and reopen; an unresolved persisted fingerprint still produced an enumerable candidate generation and force-promoted after reopen. Brain library `87/87`, every brain integration binary, all node package groups, and runtime contracts `90/90` pass. The production release then settled row `336384`: a `10,816`-entry fingerprint overlay became one new generation, both posting overlays returned to zero, all `688,026` neurons slept, available RAM rose from `6,409,084,928` to `8,974,471,168` bytes, and the same tick/topology reopened before training resumed. | `fingerprint_posting_indexes`, `fingerprint_candidate_indexes`, `FINGERPRINT_HASHED_SCALAR_GENERATION_KEY`, `FINGERPRINT_CANDIDATE_GENERATION_KEY`, `crates/brain/src/brain.rs`, `crates/brain/src/store/wbrain_store.rs`, and the row-`336384` resource settlement in the active runtime's `curriculum-health.jsonl` |
| 2026-07-28 | Correct computation shape is still insufficient when it is applied to the wrong data source. A protected prompt requested the arithmetic mean of a supplied list, but a learned `avg_resp_time(self)` method correctly computed a mean over `self._runtimes`; the behavior matched while the input contract did not. Encode collection-input grounding independently from the mean operation, then require a declared non-receiver parameter to participate in the aggregation. This distinction allows a legitimate stateful “average response time” request while rejecting it for “average of a list,” and it generalizes to future behavior checks: validate operation, source, output, and edge-condition contracts as separate co-firing features. | The row-`376832` failure isolated interval `360448:378880`. Its exact stateful candidate and the earlier temporal midpoint are rejected on normal and derived routes; `sum(values) / len(values)` and `np.average(values)` remain accepted. Instruction-intent tests pass `32/32`, all node package groups pass (`1`, `28`, `29`, and `59`), and runtime contracts pass `90/90`. After protected deployment, the row-`344064` production canary passed accepted-history recall `8/8`, toddler `32/32`, K–12 `16/16`, OOV `3/3`, and trained/unseen Python execution `5/5 + 5/5` while concurrent training advanced to row `345344`. | `crates/brain/src/pool.rs`, `crates/brain/tests/instruction_intent_encoding.rs`, `crates/node/src/brain_api.rs`, and the row-`376832` failure plus row-`344064` corrected-release records in the active runtime's `curriculum-health.jsonl` |
| 2026-07-28 | Input grounding must bind the whole requested collection, not merely an indexed slice whose text begins with the same parameter name. A scientific `sbar(Ss)` routine passed the first grounding guard because `np.average(Ss[j])` shared the prefix `np.average(Ss`, yet it averaged six separate columns and returned `(degrees_of_freedom, sigma, averages)` instead of one list mean. Treat operation, source cardinality, output cardinality, and edge behavior as independent contracts: statistics calls must receive the complete declared argument at a delimiter boundary, a scalar request must reject a top-level multi-value return, and an explicit empty-to-zero request must tie an emptiness check and zero result to that argument. A library call alone is not an empty-input policy. | The row-`393216` canary isolated interval `380416:394752`. The exact sliced scientific shape, stateful mean, temporal midpoint, unguarded NumPy mean, and multi-value summary are rejected; guarded scalar `sum/len` and NumPy means remain accepted. The focused family test passes in all node targets, all node package groups pass (`1`, `28`, `29`, and `59`), and runtime contracts pass `90/90`. After corrected-release deployment and exact guard restore, canaries at rows `360448` and `395008` both passed accepted-history recall `8/8`, toddler `32/32`, K–12 `16/16`, OOV `3/3`, and trained/unseen Python execution `5/5 + 5/5`; the worker skipped both quarantined average families and advanced durably beyond them. | `crates/node/src/brain_api.rs` and the row-`393216` failure plus rows `360448` and `395008` corrected-release records in the active runtime's `curriculum-health.jsonl` |
| 2026-07-28 | JSON-shaped input validation is not JSON aggregation. A customer-creation API method can validate fields and transmit a dictionary while sharing language, data-shape, and enterprise vocabulary with a request to summarize JSON orders by customer. Configure parsing, grouping key, accumulation, and output shape as separate co-firing requirements: a JSON-aggregation response must actually decode JSON and combine multiple records into keyed totals. | The first full CodeSearchNet completion gate passed only `4/5` Python enterprise paraphrases: “summarize JSON orders into per-customer totals” selected an unrelated customer-create method, while the other eleven enterprise suites passed. The exact failure is rejected on normal and derived routes, while a `json.loads` plus keyed-accumulation fixture is accepted. Node package groups passed (`1`, `28`, `29`, and `59`), runtime contracts passed `90/90`, and the corrected production guard passed Python trained/paraphrase execution `5/5 + 5/5`, OOV honesty `3/3`, corpus recall `64/64`, and the complete enterprise battery `12/12` with tick delta zero and unchanged topology. | `crates/node/src/brain_api.rs`, `python-enterprise-json-fix.json`, `csn-python-full.completion-gate.json`, and deferred intervals rooted at the row-`296448` comprehensive guard |
| 2026-07-28 | Separate anomaly discovery from anomaly repair at corpus scale. After a semantic completion failure, preserve its evidence, defer every unadmitted interval, restore the comprehensively accepted guard, and advance to the next corpus only if the deferred union covers every row from that guard through the phase boundary without a gap. Infrastructure uncertainty, durability disagreement, retention uncertainty, or incomplete coverage remains a hard stop. Final replay still admits each deferred interval transactionally through the comprehensive gate. | Forward-harvest accounting rejects partial coverage, marks a fully covered phase complete without admitting its failed neural state, and is enabled in the canonical reproducible trainer. Runtime contracts pass `91/91`; first production exercise is pending the next semantic completion failure. | `mark_phase_forward_harvested`, `--forward-harvest`, `scripts/train_programming_brain.py`, `tests/test_programming_runtime_contract.py`, and `docs/PROGRAMMING_BRAIN_REPRODUCTION.md` |
| 2026-07-28 | A verified same-phase rollback guard remains valid after it is copied back into the live path. Retain that immutable guard across automatic recovery so the next worker can reuse it; deleting it immediately forces an identical full-state copy before resumption and adds no safety. Release it when a candidate is admitted or forward harvest advances to another phase. | The first `csn-python-para5` canary correctly deferred rows `0:20224` after an unrelated NumPy least-squares routine shadowed scalar `square`. Recovery required a 40.8 GB restore; retaining the verified guard removes the otherwise mandatory second 40.8 GB copy before skipped-range continuation. Runtime contract coverage distinguishes default/manual guard release from automatic same-phase reuse. | `finalize_canary_restore(..., retain_guard=True)`, `perform_automatic_recovery`, `mark_phase_forward_harvested`, and the `csn-python-para5:0:20224` deferred event |
| 2026-07-28 | Supervising an existing corpus worker must not bypass the normal host-memory circuit. Apply the same consecutive-low-memory test, stop only at equal RAM/WAL-durable offsets, then neuron-wise sleep and checkpoint before returning to the ordinary guarded loop. If an emergency process stop interrupts an HTTP batch, reconcile the durable corpus coordinate from the accepted guard tick and recovered WAL tick before any replay; never assume the client ledger is authoritative over already committed neural events. | During the row-`131072` paraphrase canary, attached-mode inference and training contended on the single brain mutex while available RAM fell from `5.47 GiB` to `1.35 GiB` and the node reached `8.81 GiB`. Emergency restart replayed tick `749755`, exactly 247 episodes beyond ledger tick `749508`; progress was advanced by those 247 committed rows. Neuron-wise settlement then serialized all `821,521` neurons, reduced resident terminals to zero, converted the `14,191,279`-entry binding overlay into generation 9, preserved tick `749755`, and reduced node working set to about `80 MB`. Runtime contracts pass `93/93` with attached resource-settlement coverage. | attached branch of `scripts/programming_curriculum_supervisor.py`, `csn-python-para5.progress.json`, `/brain/sleep`, `/brain/checkpoint`, and the active runtime state at tick `749755` |
| 2026-07-28 | Resource protection must cover the persistence volume as well as RAM. A forward worker reached CodeSearchNet-paraphrase row `191735` with equal RAM/WAL offsets, but `D:` fell below one GiB; WAL appends and tracing then failed with OS error 112 and the supervisor exhausted its restart budget. After reclaiming only obsolete generated Rust targets, the live brain successfully serialized all `889688` neurons (`resident_terminals=0`) and checkpointed tick `809580`, proving the learned state remained recoverable. Supervisors now sample a configurable disk floor alongside memory, stop only at an advanced WAL-durable row after three consecutive breaches, settle/checkpoint the neuron-scoped state, record both pressure signals, and wait for both resources before resuming. | Production failure reconstructed, state recovered, and fail-before-exhaustion guard implemented | `scripts/programming_curriculum_supervisor.py`, `scripts/train_programming_brain.py`, and `tests/test_programming_runtime_contract.py` |
| 2026-07-29 | Mutable `.wbrain` rollback guards require an independent inode, not necessarily a second eagerly allocated byte-for-byte file. On Linux/XFS, `FICLONE` creates a filesystem copy-on-write reflink: accepted and candidate paths are distinct, later in-place slot/body writes cannot mutate the guard, and only changed extents consume additional blocks. Attempt reflink first and atomically publish it; retain the proven headroom-checked full-copy fallback on NTFS or any filesystem that rejects cloning. Format the temporary AWS training volume as XFS with reflink enabled. This removes the dominant 40–56 GB guard amplification without weakening rollback semantics or substituting a hard link. | Implementation and causal independence fixtures; complete programming runtime contracts `95/95` | `scripts/independent_snapshot.py`, `scripts/programming_curriculum_supervisor.py`, `scripts/programming_experiential_generalization.py`, `infra/aws/wizard-vision-private-training.yaml`, and `tests/test_programming_runtime_contract.py` |
| 2026-07-29 | At corpus scale, sparse feature classes can contain dozens of valid learned actions. Do not truncate that class by reinforcement/use count and only then apply deterministic request validation: popular but behaviorally wrong actions can crowd a rarer correct episode out of the bounded window. Apply language, behavior, input/output contract, explicit identifier, and environmental-success validation before the result limit. Keep exact raw episodes authoritative; this rule applies only to derived feature recall and still returns verbatim atom-grounded action bytes. | Experiment-supported by a causal fixture in which a twelve-times-reinforced median action outranked a three-times-reinforced odd-filter action before validation, while pre-limit validation recovered the correct rarer action. Core feature-composition tests `9/9`, focused node routing tests in all node targets, and programming runtime contracts `95/95` pass; production restored-brain admission is pending. | `decode_first_ranked_feature_binding_with_context_where`, `prompt_programming_response_compatible`, `crates/brain/tests/feature_binding_composition.rs`, and `scripts/programming_code_eval.py` |
| 2026-07-29 | Deterministically validated recall must also be allowed to inspect a richer learned episode when a paraphrase activates only a grounded subset of its features. The ordinary unvalidated route should retain its narrow one-extra-feature limit; only the validator-protected route may traverse a wider learned superset, and ranking must continue to prefer the narrowest compatible episode. This preserves contextual specificity during training without making a concise operational prompt unreachable later. | A production batching paraphrase activated `LANGUAGE:PYTHON + ENTERPRISE:BATCHING + GUARD:POSITIVE_SIZE`, while its correct learned episode carried additional input/artifact context and remained unreachable despite pre-limit filtering. A causal five-feature lesson/three-feature-query fixture now proves that unvalidated broad recall remains empty while deterministic validation recovers the verbatim action; core feature-composition tests pass `10/10`. Production redeployment is pending the current enterprise sweep. | `decode_ranked_feature_bindings_with_evidence_filtered` and `crates/brain/tests/feature_binding_composition.rs` |
| 2026-07-29 | Apply the same pre-limit rule to project composition as to a single source response. A project request needs a small set of independently grounded manifests or fragments, so its protected readout must return multiple structurally safe candidates before popularity truncation. Validate complete manifests for language and behavior; admit fragments only through the existing relative-path, role, source, dependency, and closed-subgraph protocol. Do not relax the ordinary unvalidated candidate window. | The restored production brain retained exact project episodes but returned empty or unrelated code for batching, platform, project, cross-project, composition, and semantic-stress paraphrases after a large corpus added many more popular actions to the same sparse classes. The plural validated route restored live project trained/paraphrase execution to `4/4 + 4/4`, cross-project composition to `6/6`, fragment composition to `2/2`, and semantic trained/held-out recall to `4/4 + 4/4`. The comprehensive accumulated-state admission then passed with tick and topology unchanged. | `decode_ranked_feature_bindings_with_context_where`, `crates/brain/src/brain.rs`, `crates/node/src/brain_api.rs`, and `csn-python-para5.row-191735.retention-gate.json` |
| 2026-07-29 | Compiler feedback is part of the experiential environment and must itself be deterministic. For .NET execution, place an empty-source `NuGet.Config` in every disposable workspace and disable persistent build servers; environment hints alone did not prevent implicit restore from waiting 90 seconds on a private host. Keep the SDK installation and TypeScript bundle in the reproducible host bootstrap rather than repairing individual benchmark suites. | The focused C# trained/paraphrase checks passed after a workspace-local configuration, while native and polyglot suites that shared the older runner both timed out. The shared offline runner removed those timeouts in production: native trained/paraphrase execution passed `5/5 + 5/5`; the complete enterprise bracket passed `12/12`. | `scripts/programming_exec_env.py`, `scripts/aws/bootstrap_training_host.py`, `tests/test_programming_runtime_contract.py`, and `enterprise-retention-8c6e4ef.json` |
| 2026-07-29 | Validate operation, source, edge conditions, and artifact contract before reinforcement rank. Generic Python shape is not enough for platform behaviors: schema migration requires a version marker plus schema change; a circuit breaker requires failure threshold, open state, and recovery timing; bounded asynchronous work requires both an asynchronous path and an actual concurrency bound; an atomic ledger transfer requires paired source/target balance mutation rather than any SQLite context manager. Prompt-specific default-deny authorization must include administrator, owner-read, and negative-path evidence. When a paraphrase does not explicitly request one function/method/snippet, a compatible learned manifest must precede a plain source candidate from the same sparse behavior class. | Live canonical-derived probes reproduced every collision: `setup_db` shadowed both versioned migration and ledger transfer, a generic retry shadowed circuit breaking, an executor map shadowed semaphore-bounded async, OAuth helpers shadowed owner authorization, and heartbeat code shadowed correlated redaction. Replaying 39 guarded canonical episodes fixed batching (`5/5 + 5/5`) but did not change those selections, proving routing/admission—not missing data—was causal. After deploying family validators and artifact precedence, platform and project trained/paraphrase execution passed `4/4 + 4/4` each with `3/3` honest OOV; the comprehensive gate passed without tick or topology drift. | `programming_behavior_compatible`, `prompt_programming_response_compatible`, `single_language_ranked_manifest`, `crates/brain/src/pool.rs`, and `csn-python-para5.row-191735.retention-gate.json` at tick `809619` |
| 2026-07-29 | Multiple requested programming languages imply a container artifact even when the natural-language prompt says “repository,” “codebase,” “alongside,” or “together” rather than the literal word “project.” Once component retrieval has produced a manifest covering every requested language, prefer it over any single-language fragment composition. A fragment can be internally valid and still be categorically unable to satisfy a polyglot output contract. | Live component-route diagnostics showed the correct `order_service.js + dedup.go`, `VersionedStore.java + ledger.rs`, and `RetryPolicy.cs + order_service.js` manifests were all retrieved and `manifest_composition_ready=true`; unrelated Python fragments nevertheless won `select_composed_artifact` because the paraphrases lacked `ARTIFACT:PROJECT`. The corrected live route passed `6/6` polyglot projects, `12/12` component executions, and `2/2` OOV honesty, followed by the full enterprise `12/12` and accumulated-state admission at unchanged tick `809619`. | `select_composed_artifact`, `intent_diagnostics.component_routes`, `polyglot-8c6e4ef.json`, and `csn-python-para5.row-191735.retention-gate.json` |
| 2026-07-29 | Treat `deferred_intervals_pending` as an expected two-pass curriculum handoff, not a service failure. A forward-harvest supervisor must still return nonzero for unresolved semantic obligations, but its persistent wrapper must inspect the durable status: enter `--replay-deferred` only for that exact state and propagate every other nonzero exit. During forward harvest, a failed comprehensive block must be fully covered by the appended half-open interval before progress may jump past it; restore the accepted guard, prove the restored brain independently, and pass explicit skip ranges to the next worker. | On the private XFS host, rows `191735:322807` passed every narrow canary but failed cross-project and semantic-stress in the 12-suite gate. The whole `131072`-row span was deferred, the reflink guard restored tick `809619` with zero resident terminals, the restored brain passed enterprise `12/12` with tick delta zero and unchanged topology, and training resumed at row `322807` with both unresolved paraphrase ranges explicitly skipped. A wrapper contract prevents the eventual deferred tail from being trapped in a systemd restart loop. | `scripts/aws/run_programming_curriculum_service.sh`, `tests/test_programming_runtime_contract.py`, `csn-python-para5.row-322807.enterprise.json`, `csn-python-para5.row-322807.idle-settlement.json`, and interval `csn-python-para5:191735:322807` |
| 2026-07-29 | A rollback-safe brain snapshot is not enough for a useful semantic quarantine: preserve the detailed gate artifacts before restoring the accepted brain. Aggregate and child-suite evaluators use stable filenames, and the post-restore verification can otherwise overwrite the rejected candidate's exact prompts, replies, and assertions with green baseline results. Store candidate and replay-attempt evidence under the interval's digest directory, record that path in the append-only ledger/health/status events, and pause a behaviorally rejected replay for repair rather than automatically repeating it. | The row-`322807` candidate's aggregate failure proved that cross-project and semantic-stress failed, but its shared child-suite files were replaced by the required restored-brain `12/12` verification. A causal unit fixture now mutates the shared files after capture and proves the interval-owned copies retain the rejected replies; the runtime contract passes `100/100` and the complete `tests/` suite passes `119/119`. | `preserve_admission_evidence`, `record_deferred_failure`, the deferred-replay rejection transaction, `RestartPreventExitStatus=42`, and `tests/test_programming_runtime_contract.py` |
| 2026-07-29 | Before attributing a scale regression to a new readout policy, run the identical binary against the last comprehensively accepted neural state. A green control localizes the regression to candidate experience; changing architecture first would confound the causal experiment. Preserve the failed candidate evidence, quarantine only the unproven half-open interval, restore the accepted guard, and require both focused and broad read-only gates before resuming. | Under the generation-fair binary, the row-`445431` candidate returned honest OOV for one word-frequency paraphrase (`5/5` trained, `4/5` novel), while the restored row-`322807` guard passed coding `5/5 + 5/5` and enterprise `12/12` at unchanged tick `809619`, unchanged topology, and zero idle residency. This localized the semantic damage to `393463:445431`. Forward training resumed from `322807` with all three suspect intervals skipped, and its first concurrent canary passed recall `8/8`, toddler `32/32`, K–12 `16/16`, OOV `3/3`, and coding `5/5 + 5/5` while another `3328` rows became durable. | `/srv/wizard/control/isolated-code-445431.json`, `/srv/wizard/control/accepted-guard-enterprise-809619.json`, interval `csn-python-para5:393463:445431`, and the first post-restore `continuous_canary` health event |
| 2026-07-29 | A bounded attention window must be fair across temporal experience generations. Filtering candidates before result truncation is still ineffective if a newer corpus overlay or posting generation consumes the entire upstream posting cap before an older correct neuron becomes visible. Reserve a first-pass share of the fixed candidate budget for every live/immutable generation, then spend unused capacity newest-first; this preserves a newly learned repair and established experience simultaneously without increasing the total neuron page-in bound. | The rejected row-`191735:322807` interval added roughly `4,366` authorization-like, `1,749` transaction-like, and `1,490` observability-like rows before cross-project and semantic-stress failed. A causal `.wbrain` fixture flushed the accepted authorization action into an older generation and filled a new overlay with `520` incompatible actions: the old implementation returned no validated answer, while generation-fair attention recovered the exact action with the total window still capped at `512`. Feature composition passed `11/11`, sequential brain-library tests `87/87`, and all node binary groups passed (`1`, `31`, `32`, and `62`). | `bounded_binding_postings_across_generations`, `binding_feature_postings`, `binding_motif_postings`, and `validated_feature_recall_remains_visible_across_a_full_new_overlay` |
| 2026-07-29 | Continuous evaluation needs a cooperative transaction boundary once scale makes an occasional bulk request slower than the inference timeout. First attempt the canary during live training to retain a contention signal; on infrastructure timeout, request a worker pause, let its in-flight HTTP batch finish and publish an equal RAM/WAL-durable coordinate, run one uncontended retry, then release the same worker. Never kill a client mid-request or infer its durable coordinate from the client ledger while the server may still be committing. | At paraphrase row `409847`, prior canaries were green but a later 256-episode request kept the growing brain busy long enough for repeated code-evaluator timeouts while no semantic assertion completed. The pause/ack protocol is bounded between durable batches, records the exact retry coordinate, and leaves semantic mismatch handling unchanged. Runtime contracts pass `103/103`. | `honor_pause_control`, `request_worker_pause`, `continuous_canary_cooperative_retry`, and `tests/test_programming_runtime_contract.py` |
| 2026-07-29 | The HTTP acknowledgement envelope must not be wider than the bounded neural lock/page-in scope at production scale. Releasing the brain every 32 episodes inside a 256-episode request protects lock fairness, but the client cannot publish its WAL-durable coordinate or honor a pause until all eight chunks finish. Use a 32-episode request with a maximum 32-episode adaptive lock scope so every scoped deserialization is independently acknowledged and observable by the memory circuit. | After the row-`426231` canary, a cold 256-episode request spent more than three minutes page-in bound at about 12% of one CPU while XFS reads briefly reached 93% utilization and the node resident set reached roughly 11.6 GiB. The progress ledger correctly remained at `429815`, proving the oversized request—not corpus parsing or compute saturation—was the unavailable control boundary. | `--batch-size 32`, `--lock-chunk-size 32`, `scripts/aws/run_programming_curriculum_service.sh`, and the canonical `train_programming_brain.py` default |
| 2026-08-01 | Zero neural residency and low process RSS are separate invariants. Neuron-wise sleep can correctly serialize the entire fabric while the allocator retains previously used pages; a memory-floor loop that only waits can therefore deadlock on memory owned by the already-idle node. At a proven equal RAM/WAL boundary, checkpoint first, require zero resident terminals, recycle the node, and admit the reopen only when tick and every stable topology count are identical. | Training safely settled at row `510775` with `resident_terminals=0`, but the node retained about `10.3 GiB` RSS and left only `4.9 GiB` host-available memory, below the `6 GiB` floor, for more than two days. A controlled reopen preserved tick `866547`, 13 pools, `958455` neurons, `957606` concepts, `858568` bindings, and `77955174` terminals exactly; available memory rose to about `15.1 GiB`, and forward training resumed with all five current paraphrase intervals skipped. The supervisor now performs this topology-proven recycle automatically before waiting on external pressure. | `recycle_settled_runtime_node`, `resource_node_recycled`, runtime-contract tests, and the row-`510775` production recycle |
| 2026-08-01 | A failed aggregate count is not sufficient quarantine evidence. Every retention family must persist the exact prompt, expected or accepted responses, actual reply, grounding decision, route diagnostics, and assertion result. Otherwise an interval can be correctly isolated but its causal collision cannot be reproduced after rollback removes the candidate brain. | The row-`453879` candidate retained toddler `32/32`, K–12 `16/16`, coding `5/5 + 5/5`, debugging/transfer, and enterprise `12/12`, but foundational OOV honesty fell to `2/3`. The preserved report contained only the aggregate, so it proved the contract failure but not whether `quasarithmetic`, `purple elephant theorem`, or `zxqv compiler` was answered. Foundation reports now retain row-level evidence for all toddler, K–12, and OOV probes, including grounding and route data for OOV failures. | `foundation_eval`, `oov_rows`, and `test_foundation_report_preserves_exact_oov_failure_evidence` |
| 2026-08-01 | Training request scope must shrink again as the addressable brain grows; a batch size that was safe at 400,000 rows is not a permanent constant. At roughly 1.02 million neurons and 73.9 million terminals, 32-episode production requests repeatedly held inference behind the neural lock, allowed available memory to fall below 3 GiB before the next control boundary, and contributed to OOM-driven service restarts. Use eight-episode HTTP and lock scopes with an 8 GiB preemptive memory floor at this scale so pause, canary, WAL acknowledgement, and neuron-wise settlement remain reachable. | Production-proven after deployment: the persistent AWS service restarted with `8/8` scopes at the exact WAL-durable coordinate, advanced into `jupyter-scientific-full` without a restart, and passed its first concurrent canary at row `16408`: accepted-history recall `8/8`, toddler `32/32`, K–12 `16/16`, OOV `3/3`, and trained/unseen Python execution `5/5 + 5/5`. Another `1784` rows became RAM/WAL durable during the canary, demonstrating that protected inference remained reachable while learning continued. | `scripts/aws/run_programming_curriculum_service.sh`, `wizard-curriculum-supervisor.service`, the `jupyter-scientific-full` row-`16408` `continuous_canary` health event, and `tests/test_programming_runtime_contract.py` |
| 2026-08-01 | A large `.wbrain` process exists before its HTTP socket does. Do not use only a stale `node.pid` plus listener absence to decide that no node exists: a cold loader may already own the same runtime through `W1Z4RD_NODE_BRAIN_DIR`. Discover that environment identity, require exactly one owner, and either stop it deliberately or refuse a duplicate launch. | AWS failure reproduced after the `8/8` deployment: recorded PID `217099` was dead while loading PID `221223` owned the 58.8 GB brain; recovery launched a competing loader, producing OOM and repeated address-in-use evidence. Runtime-scoped pre-bind adoption implemented and contract-tested. | `matching_runtime_node_pids`, `stop_runtime_node`, `start_runtime_node`, `node-aws.stderr.log`, and `tests/test_programming_runtime_contract.py` |
| 2026-08-01 | Exact sensory identity determines authority, not an unbounded working set. Feature and character-motif routes were capped and generation-fair, but ordered-sequence lookup still requested `usize::MAX` postings from every persisted generation and deserialized every returned binding. A common repeated prompt could therefore turn exact inference into a corpus-scale page-in. Apply the same fixed, generation-fair 512-binding attention window to ordered-sequence postings: reserve representation for every immutable generation and the live overlay, then spend unused capacity newest-first. | At the second `jupyter-scientific-full` canary, the paused worker had equal RAM/WAL row `32808`, yet the exact coding evaluator timed out while node RSS rose from about `2.8 GiB` to `13.5 GiB` and host-available memory fell to about `1.6 GiB`. A controlled recycle recovered about `11.7 GiB` available memory and resumed at the later published row `34632`. The causal regression builds two generations containing more than 700 bindings for one identical sequence, proves the older recurrent answer remains visible, and proves fewer than 600 total neurons page in. Brain answer contracts pass `12/12`, feature composition `11/11`, brain library `87/87`, and runtime contracts `110/110`. | `binding_sequence_postings`, `bounded_binding_postings_across_generations`, and `repeated_exact_sequence_uses_a_bounded_generation_fair_working_set` |
| 2026-08-02 | Per-posting limits do not bound the integrated working set when a readout unions many posting lists before deserialization. Eight feature lists plus eight character-motif lists can turn sixteen individually bounded 512-entry routes into roughly 8,192 binding page-ins. Accumulate posting identities and their independent co-firing counts without loading neuron bodies, rank higher cross-feature evidence first, apply one global 512-binding attention cap, and only then deserialize. Use the same operation for raw propagation, single-pool fuzzy recall, multi-pool integration, ranked/exact feature composition, and motif generalization so an alternate API cannot bypass the bound. | The first exact-sequence correction kept the repeated row-`32872` canary responsive longer, but its first code pass still timed out and host-available memory fell from about `10.8 GiB` to `4.0 GiB` as the fuzzy union hydrated. The global evidence ranker is unit-tested at 700 candidates; answer contracts pass `12/12`, feature composition `11/11`, multi-pool readout `3/3`, brain library `88/88` sequentially, and runtime contracts `111/111`. Production deployment and repeated second-canary proof pending. | `rank_bounded_binding_evidence`, `MAX_ROUTED_BINDING_CANDIDATES`, and every binding-union readout in `crates/brain/src/brain.rs` |
| 2026-08-01 | Behavioral compatibility must validate the requested output contract, not merely intermediate computation. For a Python word-frequency transformation, splitting words and incrementing a dictionary proves internal counting, but the response must also return that mapping. Framework lifecycle methods that write counts to external targets are different artifacts even when their internal loop fires the same semantic features. | The canary at row `543127` selected a Luigi-style `run(self)` method that built a correct count dictionary and wrote it through `self.output()`, so the prior segmentation-plus-aggregation validator admitted it; the benchmark then failed because no callable `word_freq` result existed. Rows `491671:543127` were quarantined and the accepted row-`453879` guard was restored. The Python validator now requires a returned mapping accumulator, direct dictionary, or `Counter`; all `31` Brain API test groups pass in each node target and runtime contracts pass `107/107`. | `python_word_frequency_returns_mapping`, `programming_behavior_compatible`, and `word_frequency_intent_rejects_generic_increment_source` |

| 2026-08-01 | Market supervision must preserve feature-pool identity. Flattening headline text into the OHLCV frame can create byte-level context, but it does not train the configured news pool or establish a news-state-to-outcome binding. Use one atomic multi-pool consolidation moment containing independently encoded OHLCV geometry, returns, flow, volatility, regime, cross-market context, news entities/state, horizon, and instrument context, followed by one outcome. | Core API implemented and contract-tested; market-scale admission pending | `/brain/consolidate/multi`, `brains/market_predictor_v2.identity.toml`, and `crates/brain/tests/multi_pool_binding_readout.rs` |
| 2026-08-01 | A first causal market micro-brain sweep rejected the assumption that more feature pools or equalized outcome presentation automatically improve unseen prediction. Across joint episodic retrieval, three- and seven-class targets, balanced/natural presentation, and independent per-pool voting, the tested single-pair folds remained unstable and had negative after-cost expectancy. Do not promote any of these configurations from in-sample recall; expand to many deduplicated pairs and require chronological plus entire-pair holdouts. | Negative causal sweep; configuration candidates rejected, not an architecture verdict | `scripts/market_microbrain_lab.py` and generated `runtime/benchmarks/market-micro*.json` |
| 2026-08-01 | Every chronological market split must reserve the forecast horizon after the final evaluation row as well as between training and test. A final fold ending at the last available bar has no observable future outcome and can silently truncate or mislabel evaluation. Split construction now requires `horizon + folds * test_rows` after the first test boundary and tests every target index against the source length. | Implemented and unit-tested | `chronological_folds` in `scripts/market_brain_experiment.py` and `tests/test_market_brain_experiment.py` |
| 2026-08-01 | Multi-pair training must not count duplicated chain exports as independent evidence. Build a canonical manifest using timestamp/price-series fingerprints, stratify liquid majors, DeFi, stable-quote, and high-volatility assets across chains, and hold out both future periods and complete instruments. A useful price brain should decompose broad market movement from instrument residual behavior rather than learning one absolute direction label across heterogeneous assets. | Active experiment design; empirical admission pending | `scripts/market_corpus_manifest.py`, `docs/MARKET_BRAIN_GHOST_TRADING_ADMISSION.md`, and the multi-pair curriculum |
| 2026-08-01 | Broad multi-pair exposure can unlock transfer that a single-pair brain does not show, but one shared market interval is not independent evidence. Training 2,560 moments from 64 pairs before a global cutoff produced `65.18%` direction, MCC `0.312`, positive after-cost expectancy, and a `20.26`-point baseline margin on 23 never-trained pairs/six withheld assets; known-pair future accuracy was only `45.47%`, ECE was `0.406`, profit factor `1.181`, and p95 was `665ms`. Treat this as a promising transferable-regime result, not admission: spread tests across the unseen interval, collapse chain/quote copies to base-asset/time clusters, and require multiple cutoffs. | Experiment-supported but not admitted | `runtime/benchmarks/market-multipair-01.json` and `scripts/market_multipair_lab.py` |
| 2026-08-01 | A newly trained overlay cannot perform neuron-scoped read-only cleanup until each neuron has an addressable serialized body. The first multi-pair control went directly from training to inference, so `finish_read_only_inference` correctly had nothing durable to discard and the fresh terminals remained resident. Run non-pruning sleep/settlement after the final training moment and before evaluation; this is both the durability boundary and the prerequisite for demand-paged inference. | Causal code-path diagnosis; settled comparison pending | `finish_read_only_inference`, `/brain/sleep`, and `scripts/market_multipair_lab.py` |
| 2026-08-01 | Market admission metrics must make abstention and capital allocation explicit. A parsed `sideways` label is not directional coverage, and missing/sideways predictions must remain in directional accuracy, balanced-accuracy, and MCC denominators. Concurrent markets share capital: drawdown is measured from the timestamp-ordered equal-weight portfolio return, not by summing every correlated position as if each received the full bankroll. | Evaluation-contract defect reproduced and corrected with unit tests | `evaluate_rows`, `tests/test_market_brain_experiment.py`, and `docs/MARKET_BRAIN_GHOST_TRADING_ADMISSION.md` |
| 2026-08-01 | The existing hourly OHLCV corpus contains complete buy/sell-flow fields, but those fields and ordinary price/candle/cross-market features do not supply a stable 12-hour after-cost edge by themselves. A leakage-resistant three-fold diagnostic over 31 usable base assets found price-only known-asset accuracy `50.61–53.44%` and unseen-asset accuracy `51.29–56.32%`; adding flow improved one known fold to `55.70%` but degraded another to `47.43%`, while the tested cross-market expansion degraded all known folds. Most folds remained negative after 20 bps. Treat this as a missing/excessively coarse information result and add independently sourced trade-count/quote-volume/derivatives/event data before enlarging the brain or repeating the same episodes. | Diagnostic ablation; conventional classifier used only to test information content, not as a candidate model | `scripts/market_signal_audit.py`, `tests/test_market_signal_audit.py`, and generated `runtime/benchmarks/market-signal-audit-01.json` |
| 2026-08-01 | A perpetual market GA must evolve behind the complete admission contract, not optimize an average accuracy scalar. Reserve calibration and untouched-final intervals, score the weakest known/unseen fold, retain failures, and require a fresh Wizard micro-brain gate before neural promotion. Feature selection, derived causal relationships, confidence scope, binding/concept thresholds, and supervised presentation count are evolvable; classifier parameters are surrogate screening genes only and are never copied into the brain. | Implemented, contract-tested, and started; threshold not yet reached | `scripts/market_evolution_service.py`, `scripts/market_evolution_brain_gate.py`, `brains/market_predictor_evolution.identity.toml`, and `docs/MARKET_BRAIN_EVOLUTION.md` |
| 2026-08-01 | Official spot/perpetual basis, premium, funding, trade-count, quote-volume, and taker-flow features are regime-dependent rather than universally beneficial. One protected unseen fold reached `57.44%` accuracy, positive after-cost expectancy, and profit factor `1.25`; other folds and known-asset sections failed. Keep these relationships as evolvable genes instead of mandatory pools, and never promote their best fold alone. | Multi-fold causal ablation; not admitted | `scripts/market_binance_public.py` and generated `runtime/benchmarks/market-signal-audit-02-derivatives.json` |
| 2026-08-01 | The first live perpetual-GA generation correctly rejected every genome. Its champion's weakest protected section was about `50.47%` accurate, with negative expectancy, profit factor below one, and one observation short of the 200-action floor. This is a valid low baseline for selection, not progress toward admission by itself. | Live generation zero; not admitted | generated `runtime/market-evolution/events.jsonl` and `champion.json` |
| 2026-08-01 | Revalidate every surviving genome when the causal dataset fingerprint changes; do not carry its old fitness into selection. After derivatives and time-aligned news expanded the live corpus to `47,654` rows across `31` assets, the old champion fell to `50.16%` weakest-section accuracy and fitness `44.39`. Selection within the revalidated population produced a generation-one champion at `53.68%` accuracy, `53.88%` balanced accuracy, and fitness `54.20`, while actionable coverage rose to `92.12%`. The candidate still had negative after-cost expectancy (`-0.177%` per acted observation), profit factor `0.89`, and negative baseline margin, so it remains quarantined while evolution continues. Dataset-fingerprint invalidation prevents apparent improvement caused by comparing genomes on different evidence. | Live generation-one selection on expanded corpus; improved but not admitted | generated `runtime/market-evolution/events.jsonl`, `state.json`, and `candidates/c158be1132746a1a.json` |
| 2026-08-01 | Every evolved microbrain must cross a non-pruning neuron-wise sleep and checkpoint boundary after its final training presentation and before calibration or future inference. The first smoke attempt reached tick `385` with `2,107,385` terminals still resident; scoring that state would measure warm training residency rather than the intended demand-paged brain and would invalidate both latency and mobile-memory evidence. The gate now records pre/post settlement topology and rejects any attempt whose settled resident-terminal count is nonzero. | Live lifecycle defect observed; corrected and contract-tested, clean rerun pending | `settle_brain` in `scripts/market_evolution_brain_gate.py` and `tests/test_market_evolution_brain_gate.py` |
| 2026-08-01 | Evolving a fixed menu of raw indicators is not sufficient feature extraction. A bounded genome may also evolve deterministic same-moment relationships, causal asset-relative rolling normalization, same-timestamp cross-market ranks, and the half-life used to weight older experience. Every expression must be canonical, bounded, reproducibly named, and rendered into the neural feature pool as an atom-grounded frame; no fitted surrogate state enters the brain. Availability-aware evaluation may advance to the newest timestamp supported by at least 75 percent of eligible assets, but must then deterministically rebuild the complete-instrument holdout. | Evolution schema 2 implemented and unit-tested; isolated one-generation and serialized-neural smoke validation in progress | `scripts/market_evolution_service.py`, `scripts/market_evolution_brain_gate.py`, `tests/test_market_evolution_service.py`, and `tests/test_market_evolution_brain_gate.py` |
| 2026-08-01 | Bound sensory history where atom-grounded concepts form. A 131,072-atom recent window containing one named 55-feature market frame produced a posting index hundreds of megabytes in size from only a few dozen examples. Route price, flow, cross-market, derivatives, breadth, news, and evolved expressions into independent 2,048-atom pools that fire together; longer memory belongs in causal derived features and cross-pool motifs rather than one oversized character-level window. | Live scale defect reproduced; multi-pool bounded identity implemented and rerun pending | `brains/market_predictor_evolution.identity.toml`, `streams` in `scripts/market_evolution_brain_gate.py`, and generated `runtime/market-evolution-native-wbrain-smoke` |
| 2026-08-01 | Create a fresh experimental brain in `.wbrain` storage before its first observation. Retrofitting a monolithic checkpoint after training required hydrating and rewriting the full fabric. In the native multi-pool smoke, 1,888,418 training terminals reduced to zero resident terminals at rest while 6,612 neurons were independently serialized; no `brain.bin` conversion occurred and the failed attempt was cleaned up. Legacy checkpoints still require the explicit non-destructive migration path. | Core contract and end-to-end gate proven; accuracy remains below admission | `load_or_build_brain`, `attach_fresh_wbrain`, `fresh_brain_uses_neuron_addressable_storage_from_first_observation`, and generated `runtime/market-evolution-native-wbrain-smoke-2/report.json` |
| 2026-08-01 | A best-recent-fold market genome is not a robust champion. Evolved causal normalization, technical histories, an expression gene, and an ExtraTrees screening phenotype raised one 28-day fold to `58.03%` known and `58.33%` unseen accuracy with positive expectancy, but its 42-day three-fold validation fell to `49.50%` in the oldest regime. Preserve the result as a lineage, not an admission. Use oldest-to-newest staged folds: reject cheaply when a deliberately lower multimetric prescreen fails, and require every complete fold for promotion. This focuses evolution on the weakest regime without letting an incomplete evaluation pass. | Micro improvement reproduced and full-fold regime specialization rejected; staged gate implemented and unit-tested | `scripts/market_evolution_service.py`, `scripts/market_evolution_validate.py`, and generated `runtime/market-evolution-micro-schema2-fast/full-4cfab4f708bc6281.json` |
| 2026-08-01 | News inputs need semantic scope before they become a useful market pool. A raw global count included hundreds of generic GitHub package advisories and reduced regulation, macro, exploits, institutions, liquidations, exchanges, stablecoins, network events, and whales to one sentiment mean. Filter demonstrably unrelated advisories, retain publication-time bounds, and expose neutral category counts plus asset sentiment acceleration. Evolution—not a hard-coded trading rule—determines whether and how each event regime predicts an outcome. | Corpus defect measured; evolution schema 3 implemented and unit-tested, robust sweep pending | `attach_news_features`, `NEWS_CATEGORIES`, and `tests/test_market_evolution_service.py` |
| 2026-08-01 | Confidence calibration and directional discovery are separate problems. Symmetric regression temperature calibration reduced protected-fold ECE from roughly `0.26–0.28` to `0.02–0.07` without moving the zero/sign boundary; multiple independent lineages then advanced from the oldest fold into the middle regime. Their weakest middle-regime accuracy remained about `52.7–53.3%`, proving that further threshold tuning would not repair the remaining information/generalization deficit. Preserve this calibration and evolve market-wide movement separately from instrument residual behavior, with genetically weighted recombination and no future aggregate exposed as an input. | Calibration causal repair proven; decomposed regression species implemented, cross-regime evidence pending | `regression_probability_scale`, `decompose_returns`, `scripts/market_evolution_service.py`, and generated `runtime/market-evolution-robust-schema3/events.jsonl` |
| 2026-08-01 | The GA and brain must meet at a developmental graph, not at fitted model weights. Evolve observable regime scope, specialist family, market/residual integration weight, causal features, multi-horizon curriculum, and attention/emergence settings; let atom-grounded regime, arbitration, and confirmed after-cost experience pools learn the actual motifs. Preserve each learner species and use isolated neural evidence only as a bounded within-stage selection signal: it cannot bypass a protected fold, untouched period, or any admission metric. | Evolution schema 6 implemented and contract-tested; live cross-regime evidence pending | `scripts/market_evolution_service.py`, `scripts/market_evolution_brain_gate.py`, `brains/market_predictor_evolution.identity.toml`, and `docs/MARKET_BRAIN_EVOLUTION.md` |
| 2026-08-01 | Calibration can expose rather than solve cross-regime directional failure. A lineage reached `59.87%` on the oldest protected slice with positive expectancy and profit factor `1.38`, while other two-fold survivors fell near `51%`; confidence softening improved ECE without changing signs. Represent the interaction structurally: route on observable context, then learn broad-market and asset-residual heads independently inside every regime specialist. Preserve this as its own species so global, decomposed, routed, and routed-decomposed hypotheses compete under the same weakest-section contract. | Schema 8 implemented and unit-tested; protected live evidence pending | `regime_decomposed_regressor`, `fit_regime_decomposed_regressor`, and generated `runtime/market-evolution-robust-schema3/events.jsonl` |
| 2026-08-01 | A bounded GA can lose a useful causal lineage when fold-stage rank, learner diversity, and calibration mature at different speeds. Reserve enough elites for every supported learner species, preserve the overall champion, and clone any positive-expectancy directional frontier above the multimetric prescreen floor at materially wider monotonic calibration temperatures. This changes neither feature graph nor score sign, and therefore tests confidence honesty without confusing it with directional discovery. The broader trigger was justified when a lineage reached `56.35%` accuracy with positive expectancy but stopped at an ECE of `0.277`; calibration-only descendants can test later regimes without claiming admission. | Controller invariant implemented and unit-tested; live descendant evidence pending | `select_diverse_elites`, `introduce_directional_frontier_variants`, and `tests/test_market_evolution_service.py` |
| 2026-08-01 | In a competitive market, the exploitable state may be the relationship among participants and their models rather than the first-order price pattern. Encode crowd direction, consensus, disagreement, leverage intensity, price alignment, and pressure acceleration as neutral decision-time observations in a separate pool. Do not hard-code contrarianism: informed consensus can continue, while crowded fragility can reverse or squeeze. Bind the participant state to regime, temporal motif, and confirmed after-cost experience so arbitration learns which behavior occurred. | Schema 9 implemented and unit-tested; protected live evidence pending | `competitive_reflexivity`, `REFLEXIVITY_FEATURES`, and `scripts/market_evolution_brain_gate.py` |
| 2026-08-01 | Once a lineage reaches deeper protected regimes, later failure is useful experience rather than a reason to resume blind search. Preserve the lineage and reserve bounded descendants that test shorter causal memory and observable regime-routed global/residual specialists. Keep random exploration and every learner species in parallel, and never use the untouched final period to choose these repairs. The live run reached all three screening folds but fell from roughly `55-57%` in earlier sections to `45.05%` weakest-section accuracy in the newest protected regime, making cross-regime adaptation the measured bottleneck. | Targeted repair controller implemented and contract-tested; descendants pending live evaluation | `introduce_regime_repair_variants`, `tests/test_market_evolution_service.py`, and generated `runtime/market-evolution-robust-schema3/champion.json` |
| 2026-08-01 | Operational telemetry is not brain state and must never own the training lifecycle. On Windows, a transient sharing violation while replacing a dashboard heartbeat stopped the controller after its generation state had been safely committed. Retry atomic replacement, make heartbeat publication nonfatal, and detect a neural gate that survived its parent so restart does not duplicate a memory-intensive experiment. | Live generation-50 failure reproduced; resilience repair implemented and contract-tested | `atomic_json`, `write_live_status`, `external_brain_gate_pids`, and generated `runtime/market-evolution-robust-schema3/service-8dd845a.stderr.log` |
| 2026-08-01 | Neuron-wise settlement is bounded maintenance, not interactive inference. A market gate trained its disposable brain but the non-pruning sleep request exceeded the ordinary 90-second HTTP timeout, so no lifecycle or accuracy report could be admitted. Allow up to 15 minutes only around sleep/checkpoint, restore the normal timeout afterward, and still require zero resident terminals before evaluation. | Live neural-gate timeout reproduced; maintenance boundary repaired and unit-tested | `settle_brain`, `tests/test_market_evolution_brain_gate.py`, and generated `runtime/market-evolution-robust-schema3/brain-gate-reports/ac25fbddaf3935d9.stderr.log` |
| 2026-08-01 | A regime specialist's routing observation is a structural dependency, not an optional feature. Crossover produced a regime regressor routed by `funding_rate` while omitting `funding_rate` from its feature vector, causing an honest quarantined evaluation failure. Genome finalization must restore the routing feature for every regime learner even when its inherited nominal bin count is one. | Generation-52 failure reproduced; genome invariant repaired and unit-tested | `Genome.finalize`, `tests/test_market_evolution_service.py`, and generated candidate `94db7c01ae2368b2.json` |
| 2026-08-01 | Neuron-wise persistence can be correct while a brain configuration remains behaviorally and operationally unfit. One 224-presentation market smoke brain serialized all `31,688` neurons and reduced `10,069,891` terminals to zero residency, but known/unseen accuracy was only `46.43%/50.00%`, expectancy was negative, and p95 inference was `4.44s/4.15s`. Require lifecycle, accuracy, and latency together; use this result to ablate attention windows and presentation density rather than treating zero residency as completion. | Isolated Wizard smoke gate; candidate quarantined | generated `runtime/market-evolution-robust-schema3/brain-gate-reports/d8c93844b8146fb5.smoke.json` |
| 2026-08-02 | A perpetual evolution process is not continuously learning if it holds one dataset snapshot forever. Fingerprint all causal OHLCV, derivatives, and news inputs at generation boundaries; when evidence changes, preserve heritable genomes but invalidate every statistical fitness and neural tie-break and re-evaluate them. A neural gate completed against an older fingerprint is useful historical evidence but cannot influence current selection or enter the untouched-final queue. | Live-refresh lifecycle implemented and contract-tested; adoption by the running controller pending its cooperative generation boundary | `dataset_signature`, `invalidate_population_for_new_evidence`, `--dataset-refresh-seconds`, and `tests/test_market_evolution_service.py` |
| 2026-08-02 | A cold production `.wbrain` process is already the runtime owner before its HTTP socket binds, while an independently launched initial-node unit may leave the prior `node.pid` behind. Discover ownership from `W1Z4RD_NODE_BRAIN_DIR`, require that identity to equal the eventual listening-socket PID, and atomically reconcile the PID file after health. After deploying the generation-bounded exact-sequence index, one cold PID loaded and resumed `jupyter-scientific-full` from durable row `51,160` to `53,256` with no duplicate node and no service restart. | Production lifecycle invariant proven on the 58.8 GB programming brain; wrapper reconciliation contract-tested | `matching_runtime_node_pids`, `scripts/aws/run_programming_curriculum_service.sh`, and `tests/test_programming_runtime_contract.py` |
| 2026-08-02 | A failed evaluator is not evidence of failed learning when the evaluator cannot open its own corpus or executable. A tracked-source sync removed generated `categorical_unified_001.jsonl`; the row-`67,424` canary then raised `FileNotFoundError` before querying retention and was incorrectly folded into a semantic interval. Classify missing evaluator artifacts as infrastructure, preserve the guarded candidate, restore the checksum-verified support bundle, and rerun the same gate. The invalid interval was explicitly resolved, training restarted from the exact row-`4,208` WAL coordinate, and the repeated canary passed accepted recall `8/8`, toddler `32/32`, K–12 `16/16`, OOV `3/3`, and trained/unseen Python `5/5 + 5/5` while another `1,904` rows became durable. | Production deployment defect reproduced, corrected, and live rerun proven | `transient_gate_failure`, `scripts/aws/stage_supporting_data.py`, `tests/test_programming_runtime_contract.py`, and the `jupyter-scientific-full` row-`16,480` health event |
| 2026-08-02 | A recovered node is expected to outlive the curriculum wrapper. Therefore the wrapper must not unconditionally pull in a separate initial-node service on every restart: that unit created a new cold loader beside the healthy recovered PID five times, and only the runtime-identity invariant prevented concurrent training. Let one wrapper adopt a pre-bind or listening runtime owner and launch a node only when both identities are absent. | Production ownership race reproduced; service topology corrected and contract-tested | `scripts/aws/run_programming_curriculum_service.sh`, `scripts/aws/wizard-curriculum-supervisor.service`, and `matching_runtime_node_pids` |
| 2026-08-08 | A retained EC2 instance's `LaunchTime` is its original creation time, not the beginning of its latest billed running interval. An absolute cost deadline derived from `LaunchTime + maximum_hours` remains expired across stop/start, so the persistent one-minute timer shuts down every explicit restart. Renew one bounded deadline from each explicit bootstrap/resume invocation; never disable the timer. | Production failure reproduced twice after the August 8 restart; renewed 192-hour deadline installed through August 16 and deployment tests pass `13/13` | `resumed_deadline_epoch`, `cost_guard_commands`, and `tests/test_aws_training_deployment.py` |
| 2026-08-08 | Quarantine is an intended product of forward harvesting, not a failed forward stage. Once every corpus row is either admitted or durably deferred, `deferred_intervals_pending` must return success only under `--forward-harvest` so the canonical trainer can enter transactional replay. Ordinary supervisor runs must still fail with unresolved intervals. | Whole-flow automation audit found and corrected the handoff mismatch; runtime/deployment suite passes | `deferred_handoff_exit_code`, `scripts/train_programming_brain.py`, and `tests/test_programming_runtime_contract.py` |
| 2026-08-08 | A reasoning agent is not a training scheduler. Healthy deterministic corpus progress should remain under the bounded supervisor; wake an agent only for a stable stopped/stale condition, the forward-to-quarantine transition, or an automated-stage milestone. Persist and cool down event fingerprints, but retry an unresolved actionable condition rather than suppressing it forever. Final acceptance needs a machine-readable external contract: for this brain it includes all corpus obligations, final neuron-wise settlement, a frozen `1000/1000` enterprise obstacle course, independent selectors, and an independently verified fabrication-capable robot-world capstone. | Event-driven Codex watcher, live no-trigger health probe, completion schema, and `132/132` runtime/deployment tests pass | `scripts/aws/watch_programming_brain.py`, `docs/PROGRAMMING_BRAIN_ACCEPTANCE_CONTRACT.md`, and `tests/test_programming_brain_codex_watch.py` |
| 2026-08-08 | A guarded block can contain zero admissible candidate rows when unresolved deferred intervals completely cover it. That is neither a passing brain nor a new semantic failure. Preserve the identical immutable accepted brain, advance only its logical corpus cursor across the fully accounted half-open span, retain every interval for replay, and continue harvesting the next block. Refuse this transition across even one uncovered row. Apply the same coverage rule at restart attachment, normal mid-phase boundaries, and phase completion. | Row `131072` Jupyter gate correctly sampled zero rows because adjacent obligations covered `0:131072`, but the supervisor repeatedly crashed trying to quarantine an empty span. Logical-only coverage transition implemented; runtime contracts pass `113/113`. In production the guard advanced logically across exactly the 48 gap-free intervals covering `0:131072`, preserved checkpoint tick `811692`, and resumed one worker at row `131072`; it reached durable row `136552` with the supervisor active and zero restarts. | `deferred_coverage_ids`, `advance_guard_across_deferred_block`, `mark_phase_forward_harvested`, `tests/test_programming_runtime_contract.py`, and the active runtime's `last-good.json` and supervisor status |
| 2026-08-08 | At accumulated-corpus scale, sparse semantic postings and raw character motifs must inhibit each other's false positives before action selection. The Jupyter candidate retained trained Python `5/5` but novel execution fell to `1/5`: correct semantic labels were present, yet raw motifs selected `chi_squared` for square and `smedian` for odd filtering while average and word frequency stayed honestly empty. Filter verbatim motif-recalled actions through the independently firing semantic and prompt contract before choosing the winner. A validated query may inspect 32 deterministic rarest-motif postings, while global binding attention remains capped at 512; ordinary unfiltered inference retains its eight-posting budget. Equal-size motif lists must be tie-broken by motif bytes rather than randomized hash iteration. | Exact production collision family captured; combined-route correction passes the complete feature-composition suite `12/12` and node prompt-contract tests. Rejected production state deployment and full `5/5` unseen execution proof pending. | `decode_best_binding_by_char_motifs_with_margin_where`, `crates/brain/tests/feature_binding_composition.rs`, `crates/node/src/brain_api.rs`, and the August 8 production code-evaluation trace |
| 2026-08-08 | A new semantic readout cannot recover an old action whose original episode never co-fired through that feature pool. Re-present the accepted raw instruction and action corpus once through the present raw + instruction-intent + action topology; this adds the missing association without replacing character atoms or training on the unseen benchmark prompts. One direct, WAL-durable pass over all 582 protected Python episodes completed in `10.2s` with `582/582` accepted and immediately changed novel behavioral execution from `1/5` to `4/5`. The fifth answer, `count_words`, was behaviorally correct but the evaluator demanded the hidden training identifier `word_freq` even though the public prompt named no function. Evaluation must enforce an identifier only when the request specifies it; otherwise execute top-level generated functions against the stated input/output contract. The corrected production evaluation passed trained/unseen `5/5 + 5/5`, the integrated foundation/debug/OOV gate remained green, and tick stayed unchanged during inference. A raw+semantic single-artifact fallback must be restricted to exactly one requested language: admitting it for a polyglot request returned only the JavaScript component before the existing manifest composer could combine JavaScript+Go or C#+JavaScript. With that boundary restored, the production brain passed polyglot projects `6/6`, component execution `12/12`, OOV `2/2`, and the complete enterprise battery `12/12` with tick delta `0` and unchanged topology. The repaired state was then promoted transactionally into the immutable row-`131072` guard at tick `812274` with zero resident terminals. After a real rollback/restart, the first automatic canary at row `201424` passed and forward training resumed past row `209088` with zero service restarts, proving recovery now retains the repair. | Production semantic backfill, behavioral evaluation, integrated retention, polyglot composition, complete enterprise retention, immutable guard promotion, and post-recovery canary proven. | `tools.training_standard.drive_corpora_brain --script code_gen_python_001 --feature-policy all`, `scripts/programming_code_eval.py`, `tests/test_programming_code_eval.py`, `has_exactly_one_programming_language`, `/tmp/e1eb1e5-integrated.json`, `/tmp/d716afc-enterprise.json`, and the production guard/health ledgers |

| 2026-08-11 | Releasing the immutable last-good guard is not the final forward-to-replay commit. The supervisor can stop after publishing end-of-phase forward-harvest progress and its proof but before publishing `deferred_intervals_pending`; a restart that ignores that proof reruns a completion sampler whose entire window is excluded by exact deferred intervals, then loops trying to quarantine an empty span. Validate the committed forward-harvest report, terminal progress offsets, accepted guard row, and identical unresolved interval coverage on restart, then idempotently resume the replay handoff without touching neural state. | AWS failure reproduced after more than 2,362 systemd restarts; the deployed handoff moved the service durably from `forward` to `replay` without changing brain or interval state | `completed_forward_harvest`, `jupyter-scientific-full.forward-harvest.json`, `jupyter-scientific-full.progress.json`, and `tests/test_programming_runtime_contract.py` |

| 2026-08-11 | `--replay-deferred` is a distinct end-of-corpus stage, not a modifier applied after ordinary forward completion gates. Let `run_deferred_replays` verify all phase offsets and then open the exact interval transaction immediately. Walking forward gates first makes a correctly all-deferred phase sample zero rows and fail forever before replay can publish its marker. | Deployed and proven live: the exact `csn-python-full:0:16640` transaction published its marker, created a fresh neuron-settled final-brain guard at tick `812274` with zero resident terminals, and advanced WAL/RAM together to replay row `6736` with zero service restarts | `run_deferred_replays`, `deferred-replay-7a9df07670a7d55a.progress.json`, `brain.last-good.json`, and `tests/test_programming_runtime_contract.py` |

## Maintenance rule

Update this guide whenever a configuration experiment finishes. Add the artifact path, distinguish correlation from causal ablation, and state the benchmark scope. If later evidence overturns a finding, move it to “Findings that failed or were revised” and replace the active recommendation. Never silently retain obsolete configuration folklore.
